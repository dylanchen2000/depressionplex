# DepressionPlex TST 共同裁决校准 SOP v0.1

状态：**首轮 11 个语义分歧已共同裁决；6 个边界可直接使用，C002 待一句话澄清，4 个长段待第二轮补齐完整边界。**

本文件把 2026-08-31 的双标共同裁决固化为校准规则与例子。数据属于
`legacy_rater / train / blind=false`，可用于 SOP 校准和训练，但不计入正式 12 例盲标 pilot。

## 一、统一口径

1. 各轨道独立判断。头颈、前肢、后肢、躯干形变和整体钟摆摆动不能互相代替。
2. `none`：没有观察到该轨道对应动作。
3. `subtle`：动作确实存在，但幅度小、持续短或证据较弱。
4. `marked`：动作证据明确，幅度、持续或运动模式足以与轻微动作区分。
5. `trunk_deforming` 和 `whole_body_swing` 是布尔轨道，只用 `true/false`。
6. 边界统一使用 0-based 半开区间 `[start,end)`：`start` 是第一帧阳性，`end` 是第一帧非阳性。
7. 两段动作之间若恢复为无动作，必须拆成多个区间，不能为方便而连成一段。
8. 先逐片依据画面判断；只有在证据仍接近时，才以“少漏真阳性”为最终平局规则。不得脱离画面机械取高。
9. 悬尾中头朝下是常态。极少数真实翻身向上单独记录在 `axis_orient`；仅头或前肢向上够、身体主轴未反转时仍是 `down`。

## 二、首轮共同裁决例库

| 片段 | 轨道 | 共识 | 当前边界/状态 | SOP 用法 |
|---|---|---|---|---|
| C001 | 前肢运动 | marked | `[776,816)` | 明显前肢动作正例 |
| C002 | 头颈部运动 | marked | 表中写 `[1183,1208)`；备注另写 1183–1190 轻微、1202–1208 明显 | marked 正例；边界需确认是否拆段及末帧语义 |
| C003 | 头颈部运动 | marked | 第二轮补边界 | marked 语义正例 |
| C004 | 躯干动态形变 | true | `[2810,2832)` | `trunk_deforming=true` 正例 |
| C005 | 头颈部运动 | subtle | 采用张咸明边界，即 `[3091,3181)` | 轻微头颈动作正例 |
| C006 | 前肢运动 | marked | `[3303,3373)` | 明显前肢动作正例 |
| C007 | 前肢运动 | subtle | 采用张咸明边界，即 `[3441,3506)` | 轻微前肢动作正例 |
| C008 | 后肢运动 | marked | 第二轮补边界 | 明显后肢动作语义正例 |
| C009 | 后肢运动 | subtle | `[5990,6006)` | 轻微后肢动作正例 |
| C010 | 整体钟摆摆动 | false | 第二轮复核完整分歧段是否全阴性 | `whole_body_swing=false` 反例 |
| C011 | 后肢运动 | marked | 第二轮补边界 | 明显后肢动作语义正例 |

## 三、C002 只需做一次小澄清

不要求重看整段视频，只需针对 C002 回复以下一种机器可执行结论：

- 一个连续区间，例如 `marked [1183,1208)`；或
- 拆成多个区间并分别给标签，例如 `subtle [1183,1191)`、`marked [1202,1209)`。

表中自然语言“1202-1208”看起来使用了含末帧写法，而正式数据使用不含末帧的
`[start,end)`，因此最终必须由复核人明确，不能由程序猜测。

## 四、第二轮完整边界复核

第二轮只处理 C003、C008、C010、C011。每个片段覆盖完整分歧段，并在前后各加 50 帧
上下文；视频右上角烧录原视频绝对帧号。复核包位于：

`/Users/dylanchen2000/Work/heavy/depression/recovered_annotations_v2.2/tst/boundary_review_round2_v1`

验收要求：4/4 行给出最终结论、复核人和日期；阳性动作可填写一个或多个半开区间；
C010 明确写“全段确认false”或给出发现的阳性区间。

## 五、进入正式 pilot 前

第二轮与 C002 澄清完成后，用最终 V2 工具做 1 例新的共同练习，不计入正式 pilot。
练习时两人先独立标注、互不可见、无预填；完成后再对照。本轮须确认两人都正常使用
`none/subtle/marked`，且不再存在未定义口径。随后才进入至少 3 个视频、12 个唯一
chamber-trial 的双人独立盲标 pilot；常见轨道 κ ≥ 0.80 后再扩量。
