# 人工复核预算清单　2026-09-03（**当日已更新：第 1–4 项全部关闭**）

> 前提：道俊说"还需要适量的复核，同事可以做，但是不能多"。
> 所以本清单**按信息量/人时排序**，从上往下做，做到预算用完就停。**不要从头到尾重评。**
> 单位换算：0.5x 倍速 ≈ 12–15 min/试次；纯目检（快进看几个点）≈ 2–5 min/试次。

> ⚠️ **2026-09-04 追加更正（道俊）**：本清单第 1–4 项的答案里，
> **"`30mg_2周` 隔间 4 从来没有老鼠"是错的——那里有老鼠**，只有 `20mg_3周-ch4` 从头到尾没有
> （人眼反复确认，以此为准）。少的那个切片是**当时另一个 agent 漏切的**，不是空隔间。
> 另外：**录像是几年前从客户拿到的、装/录/切三步都不是标注员做的、标注工具已优化到可用**
> ⇒ 本清单里任何涉及"拍摄/切片/工具改进"的建议**都不再适用**。
> **第 5 项（第三位评分员按 SOP v1.2 评 A 组 13 个，约 3 人时）不受影响，仍是唯一值得花的一项。**

## 0. 一句话（2026-09-03 当日更新）

**道俊同日目检把第 1–4 项全部关掉了，零人时花在同事身上。
现在只剩第 5 项：第三位评分员按 SOP v1.2（不是 v1.1，v1.1 那条规则已作废）重评 A 组那 13 个，约 3 人时。
第六项往下仍然不要做。**

---

## 1. 排序（从上往下做）

| # | 做什么 | 成本 | 关掉什么 | 状态 |
|---|---|---|---|---|
| **1** | 报出空场视频是哪个试次 ID | 0 | DP-026 / G10 | **✅ 已答：`20mg_3周-ch4`，且空的是这一个隔间不是整段视频** |
| **2** | 目检 `20mg_3周-ch4`：空场？中途脱落？还是真·全程不动的活鼠 | 3 min | DP-026 | **✅ 已答：一打开就空，从来没有老鼠 ⇒ `never_occupied`，不是脱落。DP-026 关闭** |
| **3** | 目检 7 个视频**最后一帧** | 10 min | 中途脱落有没有正样本 | **✅ 已答：除 ch4 那个外全部末帧有鼠 ⇒ 中途脱落正样本 = 0。G10 因此必须拆成 a/b/c 三支（DP-034），并连带暴露 DP-032 可能的假阳性** |
| **4** | 目检 `20mg_1周_1-3+20_2周1-ch3`（immobility 仅 36–43 s） | 5 min | 判据风险 | **✅ 已答：确实一直在动，确实只有 36–43 s 不动。该点从"疑似判据误解"升为已核实锚点（两人 Jaccard 0.96，全批最高），必须留着** |
| **5** | **第三位评分员按 SOP v1.2 重评 A 组那 13 个** | **约 3 人时**（13 × ~13 min @0.5x） | DP-025 + **SOP v1.2 有效性** | **⬅ 现在唯一剩下的一项**，见 §3 |
| 6 | 让张重评 B 组 13 个换第二个 ICC | 3 人时 | 只买到 DP-025 一个答案 | **不推荐**，被第 5 项严格支配 |
| 7 | 补齐 27 个试次的第二人 | 6+ 人时 | 无新门 | **不做。** 人工已降为辅助，不是主证据 |

> **额外零成本收获**：第 4 项的答案不只是"这个点能留"。道俊同时转述了同事对
> "有的手抬起来有的来不及抬"的解释——**"刚抬起来发现安静时间很短，不抬起来发现安静的时间很长，没法预判"**。
> 这一句直接证伪了 SOP v1.1 的按键规则（要求预测未来），并引出 28.6 s 偏差的完整定位
> 和一条新验收门 G11。**本轮信息量最大的一条来自同事的主动反馈，不是来自任何清单项。**
> ⇒ 下次复核时**明确请同事写下"哪里觉得规则别扭"**，这比多评几个试次值钱。

---

## 2. 空场视频：这是白送的黄金样本，不是废数据

同事 1 倍速空跑了 6 分钟（工具不能跳，见 DP-027），从人的角度是纯浪费。
**从验收角度它是这批素材里最有价值的一段。**

### 它一次性给三样东西

**① 全流程特异性硬样本（最重要）**
空隔间的**最危险失败模式**是：软件把空场判成 `immobility = 360 s`——即"完全不动"。
这在 TST 里正好是**最强的抑郁表型**，也就是说这个 bug 会**伪造药效**。
GLP 语境下这是最不能有的错误类型。

验收判据（写死，不许改）：

| 判据 | 要求 |
|---|---|
| N1 | 该试次必须输出 `TrialValidity = detached/never_occupied`（排除），**不得**输出任何 immobility 数字 |
| N2 | 9000 帧里被判为"有动物"的帧占比 **= 0**（不是"很低"，是 0）|
| N3 | 若出现幻影动物，必须报警而非静默——本项目已有两次静默兜底事故 |

**② 360 s × 25 fps = 9000 帧的纯噪底实测**
之前测分割噪声底只能用**静态高对比结构（8 帧）**或**静止时段动物（8 帧）**做代理，
而且已经吃过一次教训："选错静态物体可以让抖动门假通过"。
现在有一段**真·空隔间、真·全长**的录像：这是噪底台账里能拿到的最干净的一条，
且直接进"噪底台账"（噪底是传感器属性，可以是像素量，但**不得当判定阈值用**）。

**③ 空场 = 免费的几何标定素材**
我一直要求"每次开录前先录 10–30 s 空场"（几何标定 + 相机移动检测 + CSI 兼容模式背景输入）。
这段 6 分钟空场就是那个空场，而且长得多。胶带走廊标定可以在它上面单独验一遍。

### ~~需要确认的一件事~~ 已确认（道俊 2026-09-03）

**空的是 `20mg_3周` 视频的隔间 4 这一个隔间，不是整段视频。** 落到我事先列的第二种情况：

- **①② 成立且更强**——同一帧里既有真动物又有空隔间，是对"隔间定位 + 逐隔间独立判定"的
  直接检验，比整段全空更难通过。判据 N2 相应改为：**只统计隔间 4 掩膜区域**内
  被判为"有动物"的帧占比 = 0（不是整帧）
- **③ 打折**——背景另 3 个隔间有动物在动，**不能当纯空场噪底**。
  噪底台账里这一条要注明"同帧含运动动物"，避免下次被当成干净空场引用

---

## 3. 为什么第 5 项（第三位评分员重评 A 组 13 个）比让张重评更值

> **2026-09-03 更正（道俊指出）**：本节原写"B 组没人评过同一批"，**这是错的**。
> **徐乐彤与张（B 组，seed 210593506）本来也评的是同一批 14 个试次**，
> 拿不到第二个 ICC 的原因是**张的导出在 4096 字符处被截断**（DP-010），
> 可用配对只剩 **3 对**，不是设计上没有共同批次。
> 3 对不够算可信 ICC（CI 无意义），但**逐秒时间轴与边界分析在 3 对上照样能做**，
> 而且已经做了 —— 结果见 `分析_评分员分歧定位_2026-09-03.md` §8：
> 两人都"按得细"时，**松手侧的系统性偏移完全消失**（结束差中位 −0.080 s，仅 28% 为正），
> 逐秒 Jaccard **0.816** > A 组的 0.738。**这是 SOP v1.2 的第一份独立支持证据，零人时买到。**
> ⇒ 下面这张表里"张重评 B 组"那一列**已按更正重写**；结论（第 5 项仍严格优于第 6 项）不变。

张只剩 3 条完整记录（已抢救，恢复不了更多），所以 **B 组的第二个 ICC 拿不到了**。
直觉反应是"补 B 组"。但同样 3 人时花在 **A 组加第三位评分员**上，买到的东西严格更多：

| | 张重评 B 组 13 个 | **第三人按 v1.2 重评 A 组 13 个** |
|---|---|---|
| 得到第二个 ICC | 是 | 是（而且是 k=3 的 ICC，CI 更窄）|
| 能判断 28.6 s 偏差里谁是离群者 | **不能**——28.6 s 是 A 组（王娟×陈璇）的量，B 组重评不含这两人，换不出谁偏 | **能**——三个人评同一批，立刻看出是王娟偏高还是陈璇偏低 |
| 检验 SOP v1.2 的按键规则有没有用 | 弱：只能与 B 组自己的 v1 口径比，且 B 组两人本来就都按得细（见 §8），改进空间小 | **能**——这是唯一能在 A 组那条坏规则的现场做对照的机会，零额外成本 |
| 与既有 0.818 直接可比 | 不可比（跨组禁比：A 做 10mg/20mg，B 做 30mg，差异是剂量+评分员混合物）| **直接可比，同一批 13 个试次** |
| **逐秒轨迹检验新门 G11** | **B 组已经免费给了 3 对**（Jaccard 0.816，fine×fine），再重评买到的是把 3 对补成 13 对——有价值但不解决"谁偏"| **能，且是第三条独立轨迹**：G11 门槛现在有 0.738(n=13) 与 0.816(n=3) 两个估计，第三人立刻再给 13 个配对 |

### 第 5 项的执行要求（不照做则白花）

1. **必须是没参与规则设计、没看过软件输出的人**（否则锚定偏差从工具层转到人层）
2. **用 SOP v1.2**（**不是 v1.1** —— v1.1 的按键规则已被证明不可执行且已作废），
   并在备注里写明 `SOP=v1.2`。既有 43 个试次仍按 v1 口径报告，**不追溯**
3. **倍速固定 0.5x 全程不变**（陈璇批内从 0.5x 改到 1.0x 造成了 DP-017）
4. **seed 必须换一个新数字**。同组共享 seed ⇒ 顺序完全相同 ⇒ 练习效应是**共同偏差不是噪声**，
   无法回归掉。改一个数字，零成本
5. 开工前先和他一起标 1 个**练习试次**并讲透三条定义（练习不计 κ）：
   整体刚体摆动（全身荡但形状不变 ⇒ **不算** mobility，这是我们对 CSI 的核心差异化）、
   后肢有动作 vs 仅前肢小动作（金标准：仅前肢不计 mobility）、前爪抓尾（出现即勾，不判时长）

### 预期读数（现在有了明确的方向性预测，可当假设检验用）

依据 `分析_评分员分歧定位_2026-09-03.md`：偏差全在"松手"一侧，v1.2 要求"一停就松"
⇒ **第三人应当靠近陈璇（细颗粒度那一侧），而不是落在正中间。**

| 预测 | 若成立 | 若不成立 |
|---|---|---|
| 与陈璇的偏差 **<** 与王娟的偏差 | v1.2 把口径钉在细颗粒度一侧，机制解释成立 | 还有第三种未识别的分歧源，DP-036 的结论要重做 |
| 运动**起始**时刻与两人都接近（中位差 < 0.3 s） | "按下是反应"成立，DP-037 的目标函数变体有依据 | §2.3 的边界分析需重做 |
| \|差\| 仍在中等活跃度最大，但**幅度显著下降** | 倒 U 形来自"需要预测的次数"，v1.2 消除了预测 | 倒 U 形另有来源（如中等活跃度画面本身更难看）|
| 三人 ICC 明显高于 0.818 | 0.818 主要是那条坏规则的产物，**G7 门槛可能上调** | 0.818 是任务固有噪声，门槛不动 |
| 逐秒 Jaccard > 0.738 | **G11 门槛可上调**，对外口径更强 | 0.738 是人的时间轴分辨极限，门槛不动 |

**五条全是零额外成本的读数**，重评一做完立刻能算（`cli/scorer_disagreement.py` 直接跑）。
若第三人又是一个全新的量级 ⇒ **颗粒度是系统性问题**，DP-016 校准会必须开。

---

## 4. 明确**不要**做的

| 不要做 | 理由 |
|---|---|
| 重评已有的 43 个试次 | 中途改口径会在同一位评分员身上造成前后断裂，比现在的评分员间差异更难处理。v1 那批留档不动 |
| 为了"数据完整"补齐 27×2 | 人工已降为辅助不是主证据。人时应该花在**原语标注**（见 DP-029）而不是重复的整体评分 |
| 让评分员看软件输出后再复核 | 直接污染唯一的验收真值。盲法在工具层已强制，别在人这层破功 |
| 用 0.25x | 25 min/试次（0.5x 是 12–15 min），SOP v1.1 起明确不建议。**更正**：原写"四人都自发迁离了它"是错的——B 组徐乐彤与张在那 3 个共同试次上**用的正是 0.25x**（已核对导出字段）。迁离的是 A 组。理由改为纯成本，不是"大家都不用" |
