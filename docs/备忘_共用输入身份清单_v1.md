# 备忘 · 共用输入身份清单 v1（DP-133）

2026-09-18。

- 表：`docs/共用输入身份清单_v1.csv`（**158 行 × 25 列**，其中 68 行是视频）
- 生成器：`scripts/dp133_build_input_identity.py`（只读本机授权目录，不写素材）
- 守卫：`tests/test_input_identity_manifest.py`（14 条，已进 `run_tests.py`）
- 用它的两条线：**A 线** Spec A（FST 独立研究链）、**B 线** Spec B（CSI 定向验证）

---

## 一、为什么要有这张表

**文件名在本项目不是身份。** 这一条不是原则，是量出来的：

| 现象 | 实测 |
|---|---|
| `10mg 2周.mp4` 在游泳和悬尾各有一段 | 同名的两段：FST 9562 帧 / 382.48 s，TST 9661 帧 / 386.44 s，**sha256 不同** |
| `20mg 2周.mp4` 同上 | 同名跨范式，两段不同录像 |
| `zztest01.mp4` ≡ `正常5+抑郁1-3.mp4` | 转码目录里**同哈希异名**（一份字节两个名字） |
| 四个 CSI 输出目录 | **71 行记录只有 53 份互不相同的字节**（见 §五.1） |

现有登记有三处，各自都不够当身份用：`docs/CSI输入视频映射表.csv` 只有**源 sha256 前 16 位**、
没有转码件自己的哈希；`~/Downloads/tst_30clips/{manifest,cut_provenance}.csv` 以文件名为键；
`data/human_scores/manifests/*.csv` 以 `(trial_id, video_filename)` 为键、**没有视频哈希列**。
这张表把三处对不上的那部分一次性量齐：全量 sha256、帧数（含解码计数）、帧率（真帧率与场频分列）、
容器时长、来源哈希、t0、几何绑定、人工记录指针、用途组、证据级别。

**这张表刻意不做的事**（与 Spec A §3 / Spec B §9 对齐）：

1. **不建第二套人工真值表。** 人工记录的拥有者是 `data/human_scores/manifests`；
   本表只写**指针**（`human_record_ref` + `human_record_status`），不复制 holds、不复制评分、
   不复制复评归属。缺的哈希列去 DP-082 入库侧补，不在这里另立一本。
2. **不产生任何数值结论。** 表里没有秒数、没有百分比、没有阈值。
3. **不进验收路径。** `csi_read_back_verified` 仍是 `False`；本表不改变任何冻结常量、
   不改 TST 的 θ_mob / ASSAY_WINDOWS / CSV 字段 / 历史数据 / 验收门。
4. **缺失的身份不写成空行假装存在**（见 §六）。

---

## 二、授权根目录（11 个，缺一即拒产）

生成器开头逐个检查，**任一根目录缺失就 `SystemExit`，不产半张表** —— 静默少扫一个目录
比报错危险得多（少 43 行的表看起来还是一张表）。

| 键 | 路径（`~/` 起） | 进表内容 |
|---|---|---|
| `fst_source` | `Work/heavy/depression/游泳` | FST 源片 7 段 |
| `tst_source` | `Work/heavy/depression/悬尾` | TST 源片 7 段 |
| `csi_in` | `Work/csi_in` | CSI 用转码件 16 个（14 主格式 + 2 fallback） |
| `tst_clips` | `Downloads/tst_30clips` | T1 精标切片 27 段 + 2 份登记表 |
| `fst_transcode_intermediate` | `Work/heavy/depression/游泳/转码_forCSI` | 转码中间件 11 个 |
| `csi_out_fst` | `…/9月10日集中标注/CSI分析强迫游泳数据` | 43 个 CSI 输出 |
| `csi_out_ctrl` | `…/CSI分析强迫游泳数据_对照参数` | 7 个 |
| `csi_out_rest` | `…/剩余游泳视频数据` | 14 个 |
| `csi_out_norm14` | `…/正常1-4对照数据` | 7 个 |
| `ui_shots` | `…/9月10日集中标注` | Settings 面板截图 7 页 PNG + 1 份实录 md |
| `exe_zip` | `Downloads/Depressionscan  HR.zip` | 2 个原 EXE（**只哈希，永不执行**） |

外加仓库内的 6 个夹具（`tests/fixtures/csi_fst/`）与 4 份登记表文档。

**换机器跑不出来是正常的**：这些素材本来就不在 CI 上。所以守卫测试只读**已提交的 CSV**，
只重哈希 3 个 <2 KB 的夹具，CI 上没有素材也能跑（见 §七）。

---

## 三、列口径与状态词表

25 列分六组。**状态词是口径，不许当形容词用**：写进表里的每个值都在下面有定义，
新增取值必须先改这里。

**① 身份**：`material_id`（`<来源>:<范式>:<文件名>`，全表唯一）、`assay`（FST/TST/na）、
`role`、`path`（一律 `~/` 起，不写绝对用户路径）、`bytes`、`sha256`（全量 64 位小写）。

**② 时间基准**：`codec`、`width`、`height`、`frames`、`frames_source`、`fps_rational`、
`fps_field_rate`、`duration_s`、`frames_x_fps_ok`。

- `frames_source=container`：容器自己写着帧数（源片 mp4、mjpeg avi），52 行。
- `frames_source=decode_count`：容器**没有**帧数，用 `ffprobe -count_frames` 的
  `nb_read_frames` 逐帧解码数出来（MPEG-1 转码件），16 行。
- `fps_rational` = `avg_frame_rate`（**真帧率**）；`fps_field_rate` = `r_frame_rate`
  （MPEG-1 上是**隔行场频**）。两列分开记，就是为了让 §五.2 那个坑钉得住。
- `frames_x_fps_ok`：`container` 行判 `|n − d·fps| ≤ 0.5`；`decode_count` 行判
  `0 ≤ n − d·fps ≤ 1.5`（**方向有意义**：帧数少于时长×帧率 = 掉帧 = 转码事故，必须红）。

**③ 来源**：`derives_from_sha256` + `derives_from_status`
∈ `confirmed`（`cut_provenance.csv` 逐段记了源全量哈希，27 行切片）/
`confirmed_by_mapping_table`（仓库映射表点名，14 行转码件）/
`unknown_provenance`（**没有任何记录**：11 个转码中间件 + 2 个 fallback 转码件）/ `na`。

**④ t0**：`t0_source_s`（一律空）+ `t0_status` ∈ `missing_not_obtained`（68 个视频行）/ `na`。
见 §六第 1 条：**空是声明出来的，不是漏填。**

**⑤ 几何与人工记录**：`geometry_binding`（如 `10mg 2周.CLB`）+ `geometry_binding_status`
∈ `bound_by_filename_only`（25 行，**只按文件名挂，没有任何人工确认**）/ `na`；
`human_record_ref` + `human_record_status` ∈ `pointer_only_joins_by_filename`（15 行，
在评分员清单里被点名过，但**只能按文件名连**）/ `none_recorded`（26 行）/ `na`。

**⑥ 用途与证据**：`usage_group` ∈
`historical_or_dev_validation`（135 行，**已被研究过**：R3 全量对照、DP-053/055/059。
不得当新盲测）/ `test_golden`（6 行夹具）/ `engineering_only`（15 行：中间件、登记表）/
`forensic_never_execute`（2 行 EXE）；
`evidence_status` ∈ `artifact_checked`（150 行：实物在本机、哈希与元数据当场量过）/
`independently_reproduced`（6 行：本仓自有解析器独立复算过其内容）/
`static_recovered`（2 行：从 zip 里管道取哈希，**永不执行**）。

---

## 四、EXE 两行是怎么处理的

架构 §3.3 的裁决写进行里，不写在别处：`usage_group=forensic_never_execute`、
`evidence_status=static_recovered`、`path` 记成 `~/Downloads/Depressionscan  HR.zip!<内部路径>`。
哈希用 `unzip -p` 管道算，**不落盘、不解包整包**。两个内部路径带安装目录前缀
（`Depressionscan  HR/ForcedSwimScan-Ver2/HR-V2.0/ForcedSwimScan.exe`），裸文件名匹配不到 —— 这是实测踩过的。

两个哈希与逆向报告（`docs/逆向/2026-09-09…` §目标哈希）**逐字一致**，2026-09-18 在本机独立复算：

| EXE | sha256 |
|---|---|
| `ForcedSwimScan.exe` | `341a5ba8d9bec53455234a7872ef2bb57cff837c04efaa2ce8d6f5533e2bd054` |
| `TailSuspScan.exe` | `aa85e490abd8c605ac0ac52f874a9c1a7f679b094e6ffb8a8da346690ecbc95c` |

守卫测试把这两个值钉死（`EXE_SHA`）：取证材料被掉包，测试当场红。
**DT 一律不加载、不执行**（Spec B SPEC:167）；FSR3/TSR1 reader 默认不做（SPEC:166）。

---

## 五、量出来的三件事

### 1. 四个 CSI 输出目录**不是四次运行**（53 份字节，71 行记录）

| 目录对 | 同名同哈希文件数 |
|---|---|
| `CSI分析强迫游泳数据_对照参数`（7 个） ∩ `正常1-4对照数据`（7 个） | **6** |
| `CSI分析强迫游泳数据`（43 个） ∩ `剩余游泳视频数据`（14 个） | **12** |

- 对照参数目录多一个 `正常1-4对照更改.SET`；正常1-4对照数据目录多一个 `正常1-4对照.BMP`；
  其余 6 个文件（CLB / SET / 4 个 xlsx）**逐字节相同**。
- `剩余游泳视频数据` 的 14 个文件里 12 个与主目录同哈希，只多 2 个 BMP
  （`抑郁8-10.BMP`、`正常5+抑郁1-3.BMP`）。

**后果**：谁拿"我们有四个 CSI 输出目录"当"四次独立重复"，那是在数副本。
可用作对照的**互不相同**的 CSI 输出字节数上界是 **53**，其中 xlsx 结果表的独立份数还要更少。
这条已钉进守卫（`test_csi_output_dirs_are_copies_not_independent_runs`）。

### 2. MPEG-1 转码件的两个时基坑（都会让人把 0.6 s 读成 0.3 s）

15 个 `mpeg1video` 转码件，逐个 `-count_frames` 实测：

- **`r_frame_rate` 报 50/1，那是隔行场频；真帧率是 `avg_frame_rate`=25/1。**
  拿 50 当帧率，CSI 面板上的「Min Length Thresh = 15 帧」就从 **0.6 s 变成 0.3 s** —— 一个
  看起来完全合理的数字，错一倍。
- **解码计数与源片帧数逐帧相等**：14 个主格式转码件的 `nb_read_frames` 与映射表
  「转换后帧数」以及源片帧数**全部一致**（例：`FST_10mg_2w_x2.mpg` 9562 = `10mg 2周.mp4` 9562）。
  ⇒ **帧数是身份键。**
- **容器时长系统性偏短**：12 件短 0.5 帧（0.02 s），3 件短 1.5 帧（0.06 s）
  （`FST_20mg_1w_x2.mpg`、`FST_dep_8to10_x2.mpg`、`TST_20mg_1w_1to3_plus_20mg_2w_1_x2.mpg`）。
  ⇒ **容器时长不是身份键**，拿它当键会把"同一段视频"判成两段。

三条都钉进了 `test_mpeg1_field_rate_and_halfframe_truncation_pinned`。

### 3. 复现了备忘 §6.1 的 `.SET` 差分（证据级别升到 independently_reproduced）

`docs/备忘_CSI_FST参数与格式_2026-09-10.md` §6.1 说 `10mg 2周.SET` 与 `正常1-4对照.SET`
（都 1611 B、都 n_tanks=4）逐字节只差 **15 个字节、9 个连续段**。本轮用仓库自有
`parse_set` + 独立字节差分复算，**结论一致，并给出了分解**：

- base 之后（`rel+43..49`）差 **7 个连续字节** = `struggle_esc_thresh` 0.11→0.15 与
  `float_immobile_thresh` 0.09→0.12 两个 float32 的尾数；
- 表头里差 **8 个字节** = 4 个孔位三元组的 contrast 对：(80,70)→(70,60)、(70,60)→(60,55)、
  (80,80)→(70,70)、(60,60)→(75,70)；
- 8 + 7 = **15 字节**，1 + 8 = **9 段**。其余 1596 字节全同，13 个 Motion 数一字未变。

顺带量到 `正常1-4对照更改.SET`（1527 B、n_tanks=1）与 `正常1-4对照.SET` 的差别：
Motion **idx5..idx12 共 8 个数**全变（15,15,10,20,5,10,20,5 → 18,18,15,25,8,15,25,8），
idx0..idx4 一字未变，两个阈值浮点也一字未变；另有 48 字节长度差与 4 处小整数差落在尾部（见 §六第 5 条）。

---

## 六、缺失清单（表里**没有**，也不假装在表里）

| # | 缺什么 | 状态 | 影响 | 最小下一步 |
|---|---|---|---|---|
| 1 | **t0（入水时刻）** | `blocked_decision`（DP-057 / DP-074） | FST 的三个时钟里只有一个（视频帧时钟）存在；行为秒数无法与"入水后第几秒"对齐 | 要么客户给出入水时刻/带打点的录像，要么明确"FST 只报相对时间、不报绝对时刻"。Agent 不许自己造 t0 |
| 2 | **FSR3 / TSR1 逐帧文件样例** | `blocked_decision`（Spec B SPEC:166 默认不做） | 没有逐帧输出就无法做逐秒时间轴对账（G11），也无法验 CSI 的判定顺序 | DP-126 已出单要一份（任意 1 段素材即可）；要 reader 需单独授权 |
| 3 | **CSI 的 TST 侧输出** | `missing_not_obtained` | 本机只有 FST 侧的 SET/CLB/xlsx；TST 侧只有源片与切片，B 线的对照做不了 TST | 向客户要 TST 侧一次运行的 SET + CLB + xlsx |
| 4 | **正式标定件（calibration）** | `missing_not_obtained` | 几何只有 `bound_by_filename_only`：CLB 与视频之间只有文件名这条线，没有任何一份被人工确认过 | 一份带孔位真值的标定素材 + 一次人工确认；在此之前所有几何绑定不得称"已确认" |
| 5 | **`.SET` 尾部未建模区** | `artifact_checked`（量到，未解释） | 两份文件尾部差 48 字节（= 3 × 16），其中 `rel+1479` 在 4 孔件读作 4、1 孔件读作 1（与 n_tanks 一致，单点巧合，不足以定字段）。备忘 §7.3 的"每孔 16 字节数组"是**未证实**假设；本轮新量到的一点：两文件在该区**不能逐孔对齐**（4 孔件是 block1+7、block2+11 各一个 0x04；1 孔件是 +3/+7/+15 各一个 0x01）⇒ 那个朴素假设更站不住了 | 需要一份 **n_tanks=2 或 3**、其余不动的 `.SET` 才能定这段的步长；或授权 readback |
| 6 | **可当新盲测的素材** | `missing_not_obtained` | 135 行标 `historical_or_dev_validation`：手头素材全部已被研究过。再拿它们当盲测就是自欺 | 新素材（未参与过任何一轮口径制定）；在此之前只能做工程验证，不能做发布资格验证 |
| 7 | **`~/Desktop/tst_A` / `tst_B`** | `gone` | 早前盘点引用过这两个目录，现已不存在 | 无（历史引用一律以本表为准） |

---

## 七、守卫钉了什么（14 条，逐条给理由）

| 测试 | 防的是 |
|---|---|
| `test_header_is_pinned_schema` | 加减列必须改测试 ⇒ 逼出一次显式评审 |
| `test_row_floor_and_sha_format` | 少扫一根目录（行数下限 120）、哈希写成 16 位或大写 |
| `test_aliasing_is_exhaustively_explained` | 重复哈希必须落在三类已登记的别名组里，数目钉死（1 / 6 / 15）；出现第四类 ⇒ 有人复制了素材或表串了 |
| `test_csi_output_dirs_are_copies_not_independent_runs` | §五.1：6 与 12 这两个数、71 行 / 53 份字节的上界 |
| `test_same_name_across_assays_never_same_hash` | 同名跨范式必须是两段不同录像（文件名冒充身份） |
| `test_agrees_with_csi_mapping_table` | 与仓库映射表逐行互核：前 16 位哈希、原帧数、时长、尺寸、转换后帧数、derives 等于源全量哈希 |
| `test_mpeg1_field_rate_and_halfframe_truncation_pinned` | §五.2 的三条（场频 50/1、真帧率 25/1、解码计数、容器时长只许短 0.5 或 1.5 帧） |
| `test_unregistered_transcodes_are_pinned_at_two` | 映射表之外的转码件只许是那 2 个 fallback；第 3 个 ⇒ 有人往 `csi_in` 塞了没登记的文件 |
| `test_clips_derive_from_known_tst_sources` | 27 个切片的源哈希必须落在 7 段 TST 源片里 |
| `test_frames_x_fps_self_consistent_everywhere` | 任何一行 `frames_x_fps_ok != 是` 即红 |
| `test_t0_declared_absent_not_silently_empty` | t0 全缺是**声明**（`missing_not_obtained` / `na`）。哪天有人补上 t0，这条会红，逼他改口径而不是偷偷填 |
| `test_no_confirmed_geometry_and_no_fresh_blind_claim` | 不许出现"几何已人工确认"，不许有素材自称可当新盲测 |
| `test_repo_fixtures_are_true_copies` | 3 个小夹具当场重哈希，必须与客户侧原件同哈希（夹具=真拷贝，不是仿制品） |
| `test_exe_hashes_match_reverse_report_baseline` | §四的两个 EXE 哈希；`forensic_never_execute` + `static_recovered` 两个状态词也一起钉 |

**守卫只读已提交的 CSV**，不重哈希大文件（3 个 <2 KB 夹具除外），所以 CI 上没有素材也能跑。
表本身的复现要在有素材的机器上跑生成器。

---

## 八、复现方法与踩过的坑

```bash
/tmp/dpx311/bin/python scripts/dp133_build_input_identity.py \
    --out docs/共用输入身份清单_v1.csv
# 自检通过：158 行，其中视频行 68
```

坑（都是本轮实测踩到的，写在生成器注释里，这里汇总）：

1. **不要用系统 `python3`**（3.9.6）：仓库测试基线在 3.11 上是 629 通过 0 失败，
   系统 python 会给出 52 个假红。
2. **映射表带 UTF-8 BOM**：必须 `encoding="utf-8-sig"`，否则首列名读成 `'﻿范式'`，`KeyError`。
3. **`-count_frames` 是全局选项**，必须放在 `-select_streams` **之前**；且计数落在
   **`nb_read_frames`**，`-show_entries` 里不点名它就永远读不到 —— 第一次生成时
   16 行转码件帧数全空，而自检**一声不响**（它只检查有帧数的行）。已加两道自检：
   探针跑过（有帧率）却没帧数 ⇒ 停机；视频行数 ≠ 68 ⇒ 停机。
4. **zip 内路径带安装目录前缀**，裸文件名 `unzip -p` 匹配不到，会得到空哈希 `e3b0c44…`。
5. **建字典时键要带 assay**：只按文件名建 `源片名 → 时长` 的字典，TST 的 `10mg 2周.mp4`
   会盖掉 FST 的同名文件，于是 0.02 s 的半帧截断被读成 3.98 s 的差。守卫测试自己先犯了这个错，
   被同一批实测抓出来（已改成 `(assay, 文件名)` 复合键）。

---

## 九、下一项最小动作

1. **合入这张表**，然后 A/B 两线的一切引用都改用 `material_id` + `sha256`，不再用文件名。
2. **B 线**：`正常1-4对照.SET` 上做一份**只改 Motion idx5=20 / idx6=40** 的探针 `.SET`
   （`depressionplex/csi/set_write.py` 已有能力，写完自校验、逐字节差分、
   `csi_read_back_verified=False`），连同一页作业单交给有 CSI 的那台机器：
   面板加载后看**哪一侧显示 20**，一次定死 `MOTION_PAIR_UNRESOLVED_IDX` 里 (5,6) 的高低归属。
   —— 现有 3 份互不相同的 `.SET` 里，4 对同类型字段的两侧**全部相等**
   （15/15、15/15、18/18；(7,10)、(8,11)、(9,12) 同样相等），
   所以这个归属**不可能**从现有材料解开，不是"还没做"，是"材料里没有这个信息"。
3. **A 线**：FST 素材现在跑的是 TST 的几何与规则（`runner.py` 在没有 tape corridor 时拒绝出数），
   分割诊断不依赖 t0，可以先推进；t0 与发布资格按 §六第 1 条走决策请求。
4. §六的第 2/3/4/5/6 条**全部是决策或素材请求**，已集中提交（见第一轮盘点备忘）。
