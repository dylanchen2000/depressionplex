# ~~给同事的任务：步骤复核~~　**整单作废（2026-09-04 出单当天即废，未发出）**

> **不要发给任何人。** 本文件只作存根，说明它为什么错，避免以后有人翻出来照着做。

## 作废原因（道俊 2026-09-04 当日纠正，前提全错）

| 原单内容 | 为什么错 |
|---|---|
| 问同事"4 个隔间按什么顺序装老鼠""开录前会不会清点" | **录像不是我们录的**，是**几年前从客户拿到的**存量视频。我们对拍摄环节零控制，问了也没有落点 |
| 问同事"切视频用什么工具、文件名怎么起、发现空隔间怎么处理" | **切视频不是同事做的，是当时另一个 agent 做的**（现已关闭）。装老鼠 / 录像 / 切视频**三步都不是标注员做的** |
| 请同事目检 `30mg 2周` 的隔间 4 是不是空的 | **已定案，不需要再看**：道俊人眼反复确认**那里有老鼠**，只有 `20mg_3周-ch4` 从头到尾没有。**不许再花时间论证** |
| 请同事"主动挑刺"说工具哪里烦 | **标注工具已反复优化，现在可以了。**不要再问 |
| 把"抬手快慢"当可通过 SOP 消除的问题 | 那是**这种实验方法固有的 bug**，只能靠多做练习变熟，不是规则能修掉的 |
| 追查"隔间 4 是不是系统性容易被跳过" | **不追究。**"有就分析，没有就不分析" |

## 正确的去处

- 切片来源与 `ch` 编号方向的核验 ⇒ **DP-042**，**纯代码、零人时**，不需要同事
- 空隔间的处理 ⇒ **DP-043**：由人在录入时声明，声明为空的不进分析
- 唯一仍值得花人时的同事任务：复核预算清单**第 5 项**（第三位评分员按 SOP v1.2 评 A 组 13 个，约 3 人时），与本轮无关，需要时单独派

## 留下的教训（写给我自己）

**派人力任务前先确认这件事是谁做的。**本单四件里有三件问错了人，
根因是我默认"实验流程 = 同事做的"，而实际上**装/录/切三步的执行者分别是客户、客户、一个已关闭的 agent**。
同类错误已在 DP-042 里立成规矩：**任何结论都要能从原始文件复算，不许引用"某个 agent 说过"。**
