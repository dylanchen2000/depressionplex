# B15 / DP-131 复核意见 3（原文存档）

**本单适用 `docs/派工单/_共同约定_v1.md` 全文。**

> **原样存档：没有润色、没有「整理」、没有补回丢失的格式。** 补录于 DP-132（§4 第 2 件）。

## 补录说明（这一段以及下面 fence 之外的一切，都是补录时加的，不是原文）

对应 PR #108 的**第三轮**复核：「通过。合。」合入 main `67658fb`
（`gh pr merge 108 --merge`，main 只接受 merge，不许 squash、不许 rebase）。

这一份是 DP-132 的直接来源，两处：

- **第 13 条的裁决**：那句 docstring 里的条数是纯注释、CI 对它绿是正确的绿，
  不在这条分支上追 commit，「记进下一张单」，而且要办的不是把 89 改成 90，
  是**让这个数字只有一个来源并被钉住** ⇒ DP-132 §1。
- **末尾那条通则**：用出现次数当文案判据，注释一开始引用文案就会发霉；
  以后新写的文案判据一律按锚位置 ⇒ DP-132 §2 整表按锚位置搬进契约测试。

另外这一份里对 M43 的定性（「文案一字不动、只在注释里塞回原句 ⇒ node 全绿、代理 PASS，
只有契约抓得到」）就是 DP-132 concern 那句话的出处：
工具文案与自测条数的判据全部由测试钉住，不靠人手跑一遍。

原文 2256 字节，sha256 `2851f50551b50f51df645eb9b92d689a9aebd195aa00c2ca791f57fbb975807c`。原文的 markdown 围栏与行内代码标记在传抄中丢了（整份文件 0 个反引号，正文里 plaintext 一词是原来那段代码围栏——三个反引号加 plaintext——剩下的），**没有擅自补回**：补回就等于「整理」。
派工单原文另存为 `B15_0场不许开始与源码级钉子_DP-131.md`。

---

## 复核意见 3 原文（逐字节存档）

```text
DP-131 复核意见 3：通过。合。

plaintext
gh pr merge 108 --merge        # main 只接受 merge，不许 squash、不许 rebase
合完不必再回帖，分支删不删随你。

你上交的第 13 条，裁决是记进下一张单，不在这条分支上追 commit。 那句 docstring 是纯注释，不改行为、不放宽任何关，CI 对它绿是正确的绿；为一个字符付「整轮变异重跑 + 三个 CI run 作废 + 正文所有 sha/run id 跟着换」的代价，是拿纪律的形式换纪律的实质。你把它上交而不是顺手改掉，判断对——它确实动到了已核过的字节。下一张单里也不会只把 89 改成 90（那样下次加测试还会再落后一位），要办的是让这个数字只有一个来源并被钉住。

本轮认账：三个数在最终字节上重跑、CI 在同一 commit 上自己印 通过 625 失败 0；五条配套逐条落地（标签钉子 + 可见性钉子 + 0 命中取块 + 四处 count == 1 + 三样东西一字未动）；M39 两侧红、M40–M43 四颗新钉子各被打中一次、M28 从 4 红变 5 红多出来的正是可达性那条；M27–M35 红条数与上一轮逐条相同；纪律零越界。

可达性你独立推了一遍并找出唯一的口子（priors 空 + 名册外视频），用 node 第 5 条钉死——这比照抄我的推导强，我那句「必然可见」里没说到那个前提。

M43 是这一轮最值钱的东西：文案一字不动、只在注释里塞回原句 ⇒ node 全绿、代理 PASS，只有契约抓得到。它量出了「CI 三个 job 对这类错一律是绿的」这个事实，是跑出来的不是自述。你自报的第 12 条同理：三处判据差点钉成空转假绿（取块从头 index() 切出空串、夹具弄脏后不调 onPriorChange、切源码块被开栏自己的换行匹配上而切出 0 字符）——自己把假绿抓出来并报上来，比多写十条测试有用。第 10、11 条按「不改历史、如实报」处理，对。

顺带一条通则：请选择视频文件 从 1 处变 2 处是你自己新写的注释点名了它 —— 用出现次数当判据，一旦注释开始引用文案就会发霉。你改成锚位置是对的，以后新写的文案判据都按这个来。
```
