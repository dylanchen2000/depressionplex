# B15 / DP-131 复核意见 2（原文存档）

**本单适用 `docs/派工单/_共同约定_v1.md` 全文。**

> **原样存档：没有润色、没有「整理」、没有补回丢失的格式。** 补录于 DP-132（§4 第 2 件）。

## 补录说明（这一段以及下面 fence 之外的一切，都是补录时加的，不是原文）

对应 PR #108 的**第二轮**复核：只改后半句，改完就合，不会有第三轮。
落地 commit `1634acf`。

**这一份里留着架构师自己认错的那一处（原文 §6：「我点名了一个评分员当时看不见的控件」），
按 DP-132 §4 的要求原样保留、不删不改**——它是「可见性钉子」的由来：
第二轮因此要求补一颗 `rescoreBox.hidden === false` 的可见性钉子，
DP-132 §2 又把同一件事推到文案层（渲染层判据：markup 里被注释包住的话，
全文搜得到、评分员看不见，那种绿是假的）。

另一处（DP-132 §4 提到的「把 querySelectorAll 的按钮清点当成了『屏幕上有』」）
在我收到的这三份复核意见里**都没有对应字句**——我没有替原文补话，
已作为待裁事项上报（可能还有一份没交到我手上，或那一处是在别处说的）。

原文 2846 字节，sha256 `cac0afb2bf140e28dccc3280fd9b46dd45d15b3b2f19dd4ade500c57a5914b51`。原文的 markdown 围栏与行内代码标记在传抄中丢了（整份文件 0 个反引号，正文里 plaintext 一词是原来那段代码围栏——三个反引号加 plaintext——剩下的），**没有擅自补回**：补回就等于「整理」。
派工单原文另存为 `B15_0场不许开始与源码级钉子_DP-131.md`。

---

## 复核意见 2 原文（逐字节存档）

```text
DP-131 复核意见 2：你按裁决做的部分全部通过；那句文案的后半句是我写错的，只改后半句，改完就合，不会有第三轮。

§6 你上交的是实错，我认。重做已完成试次… 在 <section id="scoring" hidden> 里、只有开评后才渲染，它开的下拉列 state.done，这道关拦住的那一刻是空的——我点名了一个评分员当时看不见的控件。设置页上真对得上「勾…并在清单里勾选」的是 rescoreChk + rescorePick，跟你说的一致。

顺手把可达性核清了，省你一轮：onStart 里 priorDone 来自 readPriorDone(priors, …)，只吃「已评进度」选中的那几份、不吃 localStorage 存档。所以 nHave == 0 必然要求 priorDone 非空 ⇒ state.chainDone 非空 ⇒ rescoreBox 已经 hidden = false。这道关能触发时「重评」那一栏必然在屏幕上，所以这半句不必加条件分支，直接点名。

红字整串改成（逐字，含标点）：

plaintext
你选的视频对应的场次都已经评过了：本批没有要评的场次。请把还没评的那几场的视频选进来；确实要重做已评过的场次，就在上面「重评」那一栏勾「本次是重评（rescore）」，再在下面的清单里勾中要重做的场次。
配套五条：

引号里那两个标签从 HTML 原文取真值来比，照你给 redoBtn 那颗钉子的做法：rescoreBox 的 legend 以 重评 开头、rescoreChk 的 label 含 本次是重评（rescore），与红字引号内逐字相同，改任一侧就红。
再加一颗可见性钉子：这道关触发的那一刻 rescoreBox.hidden === false（或等价地 nHave == 0 蕴含 state.chainDone.length > 0）。这颗钉子就是抓我这类错的，别省——你那颗 redoBtn 钉子只钉了标签、没钉可见性，所以没抓住我。
重做已完成试次… 在这道关的文案里 0 命中；openRedoPanel / redoSel / redoBtn 本身一个字不许动。
M39 = 把后半句改回 请勾「重做已完成试次…」并在清单里勾选，必须两侧红；M37／M38／M32 按新整串重跑。
前半句、请选择视频文件、N ≥ 1 那一路，全部一字不动，原有判据保持。ZERO_SIG = "本批没有要评的场次" 那个子串设计是对的，留着——这轮改后半句它正好该扛住。
在 fafcaa2 上再追一个 commit，不 rebase。回帖仍是那五项。

本轮认账的：三个数在最终字节上重跑（三个文件 sha256 与变异轮基线逐字节相同）、五条配套逐条落地、M37/M38/M32 都列了实得串、M36 的复核结论正确（它红在 §1.4 第三道关那条断言要的「没选到视频」四个字不在新串里，是文案不等）、M27–M35 红条数与上一轮逐条一致、纪律零越界。§7 那两处行号只更正不改历史，处理方式对。

```
