# B15 / DP-131 复核意见 1（原文存档）

**本单适用 `docs/派工单/_共同约定_v1.md` 全文。**

> **原样存档：没有润色、没有「整理」、没有补回丢失的格式。** 补录于 DP-132（§4 第 2 件）。

## 补录说明（这一段以及下面 fence 之外的一切，都是补录时加的，不是原文）

对应 PR #108 的**第一轮**复核：一处必改（裁决① 那道关的红字在说一个不可达的状态），
改完就合。落地 commit `fafcaa2`。

这一份末尾那条「不归你、别动」正是 DP-132 的由来：
`test_stopwatch_timer_js_selftest` 只断言 rc == 0 + 「全部通过」这件事当时被按下另开一单，
DP-132 §1 办的就是它（连同复核意见 3 上交的第 13 条：那个数字要只有一个来源并被钉住）。

原文 2791 字节，sha256 `3cbd8105e7c150e8abef310b05834eb423336f4d0690d896f197d3d879bb3665`。原文的 markdown 围栏与行内代码标记在传抄中丢了（整份文件 0 个反引号，正文里 plaintext 一词是原来那段代码围栏——三个反引号加 plaintext——剩下的），**没有擅自补回**：补回就等于「整理」。
派工单原文另存为 `B15_0场不许开始与源码级钉子_DP-131.md`。

---

## 复核意见 1 原文（逐字节存档）

```text
DP-131 复核意见 1：一处必改，改完就合。其余通过（625 / 89 / 18 三个数、三个 job、M27–M36 两侧全红、范围外 0 改动、human_agreement sha256 未变，都认账；§5.3 第 3、4 条你自己更正的方式就是标准）。

必改 —— 裁决① 那道关的红字。你在 §5.3 第 1 条把文案上交，裁下来是改，理由不是措辞好坏，是它说的状态不可达：真的一个文件都没选，会被 §1.2 第 504 行的 请选择视频文件 先接住（我在走脚本场景 4A 上实测过），所以这道关唯一打得着的形态只有「选了视频、但选到的每一场都已评过」，而红字在说「你没选视频」。评分员照它做再去选一遍视频，解决不了任何事——这正是 §1.5「界面文案不说假话」要挡的形状。

改成（逐字，含标点）：

plaintext
你选的视频对应的场次都已经评过了：本批没有要评的场次。请把还没评的那几场的视频选进来；要重做已评过的场次，请勾「重做已完成试次…」并在清单里勾选。
（「重做已完成试次…」照抄界面按钮现有标签，含省略号，不许另起说法。）

配套五条，缺一条不算改完：

node 自测里 选到的视频全是已评过的场次 那条的期望串换成新文案，逐字；契约侧同步钉新串恰好 1 处。
旧串 本次没有选到任何视频 在这道关上 0 命中；§1.2 原有的 请选择视频文件 一个字不许动（那是另一道关，别顺手合并）。
新增 M37 = 把新串改回旧串，M38 = 把「都已经评过了」改成「没有」，两条都必须两侧红；M32 沿用现形状重跑。
M36 必须重跑并复核：重评那一支死掉后夹具会落到这道关上，红字随之变，要确认它还是因为「文案不等」而红，不是因为别的。这个形状是你自己在 §5.5 指出来的。
N ≥ 1 那一路文案继续一个字不动；本批只评这 " + nHave + " 场 恰好一处、本批只评这 0 场 全文件 0 命中（含注释）两个判据保持。
在原分支追一个 commit，不要 rebase。回帖给：node 条数 + 契约条数 + 3.11 run_tests.py 末行 + 五条变异各自红了哪几条 + 三个 job 的 run/job id 与 conclusion。

顺便把 HTML 那 10 行新增贴回来（git show <head> -- tools/timer/DepressionPlex_stopwatch_timer_v1.html）——我引的旧串是从你 §5.5 M36 那一栏抄的，没看过原文。

不归你、别动：test_stopwatch_timer_js_selftest 只断言 rc == 0 + 「全部通过」这件（你 §5.3 第 5 条，裁决是补，但要改 DP-128 的既有测试，我另开一单）；确认框第二行那对字面 **全部** 星号（属于删字，另一件事）。docs/ 继续一个字别动。
```
