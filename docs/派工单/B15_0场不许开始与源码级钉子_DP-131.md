# B15 / DP-131 派工单：0 场不许开始 + §1.2/§1.4 契约侧源码级钉子（事后补录）

**本单适用 `docs/派工单/_共同约定_v1.md` 全文。**

> **事后补录，正文以 PR #108 为准。**

## 补录说明（这一段以及下面 fence 之外的一切，都是补录时加的，不是原文）

补录于 DP-132（§4 第 1 件）：`docs/派工单/` 此前没有 DP-131 的任何记录，而这一单
已经合入 main（PR #108 → `67658fb`）。

1. fence 里是**当时收到的派工单原文**，按字节存档：没有润色、没有「整理」、
   没有补标点。原文 1969 字节，sha256 `1a65707232810a87c57492eb527d9bb738f5a1c63f7b65a33efe34788e4b6fb7`。
2. 为什么存原文、而不是照 PR #108 正文回推一份：PR 正文是**交付稿**（执行方写的），
   派工单是**架构师下的单**，两者不是一件事；回推会把「要求」与「结果」混成一份，
   以后没人分得清哪一句是当初的命令、哪一句是后来的自述。
   「正文以 PR #108 为准」这句照 DP-132 §4 的要求写在最上面——落地结果、变异编号、
   三个数、CI 的 run id 全在 PR #108 正文里，那一份才是权威。
3. 原文的 markdown 围栏与行内代码标记在传抄中丢了（整份文件 0 个反引号，正文里 plaintext 一词是原来那段代码围栏——三个反引号加 plaintext——剩下的），**没有擅自补回**：补回就等于「整理」。
4. B 号：`docs/派工单/` 现有最大是 B14（DP-129），DP-130 在这个目录里没有对应文件，
   所以这一份用了 B15。**若架构师要把 B15 留给 DP-130，改名即可，内容不用动。**
5. 三轮复核意见各自原样存档：`B15_复核意见1_DP-131.md`、`B15_复核意见2_DP-131.md`、
   `B15_复核意见3_DP-131.md`；对应的落地 commit 分别是 `fafcaa2`、`1634acf`、
   （第三轮是「通过。合。」，没有代码改动）→ 合入 `67658fb`。
6. 原件出处：DP-131 派工那一刻收到的消息全文（本机临时目录里那一份），
   与 PR #108 正文、`git log` 上的 commit 消息可互相对账。
7. `docs/ISSUES.md` 第 150 行的 DP-131 仍写着 `open（2026-09-16 出单…）`。
   本单没有动它——DP-132 §4 只点了四份文件，改台账不在里面；已作为待裁事项上报。

---

## 派工单原文（逐字节存档）

```text
#104 已合入 853b221，四个文件、depressionplex/+data/+docs/ 零改动、合后树与 CI 绿的 head 逐字节相同，我都独立核过了。你自报的指纹 bug 是这一轮最有价值的一条——它活在一个 CI 全绿的 head 上，且后果是真清单永远开不了评。批 B 派工前我会自己走一遍你那 12 场景脚本，不拿 CI 绿当工具能用。

五条判断我裁完了，其中两条要动代码，开一张小后续单 DP-131，分支 feat/dp131-timer-followup，从新的 main（853b221）切：

N == 0 时不许开始：本次选到 0 场视频 ⇒ 一道「不许开始」，文案「本次没有选到任何视频，选好视频再开始」。N ≥ 1 的现有文案是真话，一个字不许动。那句「本批只评这 0 场」随之消失——它不是措辞问题，是缺一道关。守卫：0 场 ⇒ 拦住（node + 契约各一道），且变异「把这道关放行」要红。
§1.2 三道关 + §1.4 在契约侧补一道源码级钉子（M25/M26 现在契约侧 0 红）。docstring 里必须写明：这道钉子只证明那道关还在源码里，不证明行为；行为归 node。理由是看守链太长——node 靠 test_timer_tool 一条代理测试代跑，代理不可用时那两道关就没人看了，而它们是「谎报第一次」和「不勾重评却派已评场次」的唯一防线。
另外三条我裁了但不归你：生成器落入库侧新模块、只出 *_draft.csv、CSV 不加 first_scored_unresolved 列（DP-130）；28 条 null 用「与 duplicate_notes 对账、对不上就停机」收口（DP-130，你那条「28 条 null 的键与 28 个 duplicate_notes 完全相同」的实测就是这条裁决的判据）；commit type 不统一、不动历史（rebase+force 是禁令）。

纪律照旧：docs/、data/ 不动；不许 git add -A；不许 rebase + force；变异逐条还原 + sha256；每条独立 PYTHONPYCACHEPREFIX；DOM 桩测试先弄脏再验清理。

```
