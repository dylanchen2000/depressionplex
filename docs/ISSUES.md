# Issue 台账（DP-###）

> **2026-09-03 更正**：GitHub remote **一直是通的**（`dylanchen2000/depressionplex`，私有，
> 8-24 已建，8-25 已 push，PR #1–#8 已合并）。沙箱经 HTTPS + `~/.git-credentials` 可读可写；
> 只有 SSH（22 端口 ssh 客户端握手超时）和 `gh` CLI（`~/.config` 不是目录）不可用。
> 本文件继续用作**中文 issue 台账**，与 GitHub PR 并行，不再声称"remote 尚未建立"。规程见 `docs/WORKFLOW.md`。
> 编号只增不复用。状态：`open` / `doing` / `done` / `blocked` / `wontfix`。

| # | 状态 | 归属 | 分支 | 标题 |
|---|---|---|---|---|
| DP-001 | done | Capy | — | 核心骨架：几何语义图 + RAD + CSI bout 流水线 |
| DP-002 | done | Capy | — | S1 阈值分割 + 采集端/噪声底体检，P2 硬门通过 |
| DP-003 | **done** | 道俊 | — | GitHub 私有仓库 `dylanchen2000/depressionplex` 8-24 已建、8-25 已 push。**沙箱经 HTTPS 可 push**（`~/.git-credentials` 有 ghp_ token，repo 全权限）。~~靠 bundle 交付~~ 不再需要 |
| DP-004 | **open（已降级）** | 道俊 | — | **比对口径已由数据自证，不再阻塞**：43 个试次中有 holds 的 39 个，**最大 hold 结束时间无一超过 360.00 s**，且多个恰好等于 `360.00` ⇒ 工具在 360 s 硬收口，评分员实际只看了文件时间 **[0, 360]**。软件按同一区间算即可，`immobility = 360 − mobile` 对这批数据成立。**剩下的是科学问题不是工程问题**：文件时间 0 是否等于"小鼠悬起那一刻"？若录制早于悬挂，则人和软件都把布置期算进去了（不伤 r，但绝对 immobility 与文献不可比）。**只需目检 7 个视频的第 1 帧，几分钟** |
| DP-005 | **blocked（优先级升高）** | 道俊 | — | 确认 `30mg_2周-ch4` 为何缺失（28→27）；若为预剔的已知脱落试次须放回。**2026-09-03 后本条成为"中途脱落"唯一可能的正样本来源**：道俊已确认除 `20mg_3周-ch4` 外**所有视频最后一帧都有老鼠** ⇒ 27 个已评试次中中途脱落**零正样本**（见 DP-034）。这一个试次决定 G10 的脱落分支能不能活 |
| DP-006 | open | 工具作者 | — | 秒表工具：hold 按视频时间插入并合并，不追加到数组尾部（4 位评分员共 12 个试次数组乱序自嵌套） |
| DP-007 | open | 工具作者 | — | 秒表工具：每按键多算 **0.121 s 墙钟**（7 个独立估计 0.110–0.131，CV 6.5%）。改在视频时钟上取 keydown/keyup |
| DP-008 | open | 工具作者 | — | 秒表工具：提供「撤销」语义；现在回看重评只能加不能减 |
| DP-009 | open | 工具作者 | — | 秒表工具：补 `completed_at`，使实际作业顺序可查（`presentation_order` ≠ 实际观看顺序） |
| DP-010 | open | 工具作者 | — | 秒表工具：导出失败时不要回退到内部状态。张的导出报错后只能贴 localStorage，**且在 4096 字符处被截断**。抢救已做完（`cli/salvage_truncated_audit.py` → `human_scores_张_..._SALVAGED_3of14.csv`）：**完整记录只有 3 条**（全 0.25x），第 4 条 `20mg_3周-ch4` 被截在 holds 中途、**已丢弃不修补**，`order` 里另外 23 个本次导出中完全无记录。**数据到此为止，恢复不了更多**——工具侧仍需修，避免下一批重演 |
| DP-011 | open | 工具作者 | — | 秒表工具：`mobile==0 且 holds==[] 且 unscoreable==false` 必须拒绝导出（区分「没评」与「评出 0」） |
| DP-012 | **代码已交付，Capy 复核通过（2026-09-03）** | 实现 agent | `feat/human-agreement` @ `53f9817` | PR 1：人工评分校验器 + 并集重算。**不算任何一致性指标**。见 `SPEC_人工比对与验收_v2.md` §9。**四条硬规则逐条核过**：① `mobile_seconds` 已丢弃（只留记账列 `mobile_seconds_DISCARDED`）② `union_holds` 先记 `unsorted` 再 `sorted(pairs)` 取并集，并报 `naive_inflation_s` ③ `_rule3_violation` 精确实现 `mobile==0 && holds==[] && unscoreable==false` ⇒ 拒收 ④ 零长段记账。**43 行重算表实测：42 accepted / 1 rejected（正好是 `徐乐彤/20mg_3周-ch4`，拒收理由写清"没评≠评出0，按0入库会得 immobility=360 s"）、12 个乱序（与我的审计一致）、虚高合计 +156.0 s 最大 +54.8 s**。两条独立交叉验证：每按键墙钟加成均值 **0.1202 s**（我独立算的是 0.121 s）；immobility 最小两条正是锚点 `20mg_1周…-ch3` 的 **36.8 / 43.0 s**，与道俊目检的"只有 36–43 s 不动"逐位吻合 |
| DP-013 | **代码已交付，Capy 复核通过（2026-09-03）** | 实现 agent | `feat/human-agreement` @ `53f9817` | PR 2：LOVO-CV 框架（7 折，只拟合 θ_mob 一个标量），先用合成真值跑通。**两条关键要求都满足**：① `FITTED_PARAM = "theta_mob"` 写死，bout 参数走 `bouts.CSI_TST_STARTING_POINT` 从不进网格，且拟合值**不回写** FROZEN；测试 `test_only_theta_is_a_search_dimension` 锁住 ② 输出 `theta_values`（每折一个）+ `theta_cv_pct`，报告里直接印 "G2 = θ_mob 跨折变异系数 = X%（门槛 ≤15%）"，测试 `test_theta_values_are_the_G2_raw_material` 锁住。另有 `test_truth_cannot_be_software_output_by_construction` 防真值自证。搜索网格 0.005–0.05 含 FROZEN 出货值 0.0175。**该分支测试 124 全绿（含我 main 上的 7 个）** |
| DP-014 | open | 实现 agent | `feat/human-agreement` | PR 3：正式 G1–G10 报告。前置：DP-004 + 配对数据齐 |
| DP-015 | done | Capy | `chore/repo-hygiene` | 分支/issue/ignore 规程；修 `.gitignore` 让人工评分文件能入库 |
| DP-016 | open | 道俊 | — | 评分口径不统一是当前最大噪声源：4 位评分员在动段中位 1.08–3.36 s，总时长 115–196 s，**同 13 个试次 ICC 仅 0.818**。要不要做一次统一口径的校准会 |
| DP-017 | open | 道俊 | — | 陈璇后 7 个试次自行改用 **1.0x** 实时速（前 6 个 0.5x）。同一评分员批内倍速变更，须定：接受并单列，还是重评 |
| DP-018 | open | Capy | `spike/hysteresis-bouts` | 评估 bout 判定改为**双阈值滞回**（θ_enter/θ_exit，参考 TopScan 开始规则/停止规则分离）。代价是自由参数从 1 个变 2 个，会削弱 G2 |
| DP-019 | open | Capy | — | 与 EthoPlex 事件规则编辑器对齐规则表达式契约（AND/OR/N-of-M + 时长约束 + 滞回），三产品线共用一套 schema |
| DP-020 | **done（早已完成，我出单时不知道）** | 实现 agent | `feat/tape-corridor` → PR #1 | 胶带走廊标定几何。**8-24 就做完并已合入 `main`**：暗频率法标定、A 批 2/4→**4/4**、B 批与基线逐位一致、7 个视频跨视频泛化全过、验收 A1 判据本身被证明写错并已修为 v1.1。测试 49→74。我 9-03 出派工单时读的是 8-24 的本地快照，未 fetch 远端，把已完成项当首要任务派出去了 |
| DP-021 | **done，但按键规则一条已被 DP-036 推翻** | Capy | `chore/repo-hygiene` | SOP v1.1 补按键颗粒度规则 + 倍速一致性 + 导出方式。**不追溯已评 43 个试次**，适用 FST/新批次/重评。⚠️ 其中"约半秒以内的停顿不松手"一条**已作废**（不可执行 + 只吃掉 6.6% 偏差，见 DP-036 / SOP v1.2）。倍速一致性与导出方式两条仍然有效 |
| DP-022 | **wontfix** | — | — | 原为 DP-020 抽帧上传。DP-020 已完成且**在 Mac 侧用全部 7 个视频验过**，本条作废 |
| DP-023 | open | Capy | — | **架构侧流程缺陷**：我 9-03 出派工单前没有 `git fetch`，用 8-24 本地快照当现状，导致派工单 A 轨（DP-020）派的是已完成的活。**规程补一条：任何出单/写 STATUS 前必须先 fetch origin 并读远端 STATUS**。已写入 `WORKFLOW.md` |
| DP-024 | **done** | 道俊 | — | **道俊 2026-09-03 目检确认：视频一开始老鼠就已经挂在那里。** ⇒ 文件时间 0 ≈ 悬挂完成时刻，计分窗口 [0, 360] 就是金标准的"悬起后 6 min"，**绝对 immobility 与文献可比**，不需要 `t_suspend` 偏移。DP-004 至此完全关闭。唯一残留（不阻塞，报告里一句话交代）：录制开机比悬挂晚多少 δ——若晚几秒，丢掉的是挣扎最密的开头，会**系统性抬高** immobility |
| DP-025 | **open（要拍板）** | 道俊 | — | **B 组第二个 ICC 拿不到了**：张只剩 3 条完整记录（与徐乐彤重叠 3 个），n=3 算不出可信 ICC。⇒ `ICC=0.818` 将是**唯一一份、未被独立复现**的评分员间一致性（n=13，CI 0.741–0.975）。三个选项：① 接受，报告里写明"单组未复现"（**推荐**，人工已降为辅助）；② 让张或第五人重评 B 组 13 个（约 3 人时/13 试次 @0.5x，换一个独立 ICC）；③ 只让张重评与徐乐彤重叠的那 13 个中的一部分。**注意徐乐彤 14 条完整数据不受影响**，G7（软件-人工 r）在 30mg 视频上的覆盖照旧 |
| DP-026 | **done（结论：never_occupied，不是脱落）** | 道俊 | — | **道俊 2026-09-03 目检确认：`20mg_3周-ch4` 一打开就是空的，从来就没有老鼠。** ⇒ 该试次是 `never_occupied`（布置/录制问题），**不是中途脱落**。四方独立一致：徐乐彤近零导出、张 `mobile_seconds:0.31`、算法侧标定拒绝、道俊目检。**G10 的脱落分支因此零正样本**（见 DP-034），never_occupied 分支得到 1 个正样本 + 27 个真负样本 |
| DP-027 | open | 工具作者 | — | 秒表工具缺两个能力，代价已实测：**① 不能跳（无进度条拖动/快进）** ⇒ 遇到空场视频只能 1 倍速空跑 **6 分钟**；**② 没有「本试次无动物/不可评分」立即提交并跳过的出口** ⇒ 只能硬看完再勾。加这两个是纯收益，且能把 DP-011（`mobile==0 && holds==[] && unscoreable==false` 必须拒绝导出）从"报错"升级为"有正确的路可走" |
| DP-028 | **open（高价值）** | Capy + 道俊 | — | **空场试次是这批素材里最有价值的一段，不是废数据**：360 s × 25 fps = **9000 帧真·空隔间**。三个用途：① **全流程特异性硬样本**——最危险失败模式是把空场判成 `immobility=360 s`，那正好是最强抑郁表型，**这个 bug 会伪造药效**，GLP 下最不能有；判据 N1 输出 `detached/never_occupied` 且不输出任何 immobility 数字、N2 判为"有动物"的帧占比**=0**（不是很低，是 0）、N3 幻影必须报警不得静默。② **最干净的噪底实测**（此前只能用 8 帧静态结构或静止动物代理，且已吃过"选错静态物体假通过"的教训），入噪底台账、**不得当判定阈值**。③ 免费的长时空场几何标定素材。**2026-09-03 道俊已答：空的是 `20mg_3周` 视频的隔间 4 这一个隔间，不是整段视频。** ⇒ ①② **成立且更强**（同帧内既有真动物又有空隔间，是对"隔间定位 + 逐隔间独立判定"的直接检验，比整段空更难）；③ **打折**（背景里另 3 个隔间有动物在动，不能当纯空场噪底；②的"判为有动物的帧占比=0"改为**只针对隔间 4 的掩膜区域**统计） |
| DP-029 | **open（要交接）** | 道俊 → Capy | — | **同事已做了一批逐帧原语标注 + 小批 FST 原语标注试验，文件还没进仓库。** 入库前必须问清五件事，否则数据无法用作真值：① 用的是 `depressionplex.cli.annotate` 还是别的（Excel/CVAT）？② **是否用了机器输出预填**（`--from-events`）、标的是训练池还是验证池——预填污染验证真值是不可逆的；③ 采样方案（逐帧全标？每 N 帧？只标片段？）——6 min×25fps=9000 帧手标不可能，必须知道实际覆盖；④ 哪些试次/帧区间；⑤ 是否两人标过同一段（有重叠才能算 κ）。**FST 那批另需确认**：计分窗口是"6 min 里的后 4 min"（与 TST 全程 6 min 不同，不许抹平），且 FST 画面里有 TST 遗留悬挂钩须几何排除 |
| DP-030 | done | Capy | `chore/human-scores-sop-workflow` | 原语表版本化：`PRIMITIVE_TABLE_VERSION="primitives-v1"` 写进标注文件；存量无版本文件按 `pre-v1` 告警接受**不拒收**（同事那批是真实真值）；`agree` 跨版本算 κ 直接拒绝（混淆定义差异与判断差异）。测试 100→103 |
| DP-031 | **open（已从"可选"升为"必做"）** | Capy | — | `validity.py` 现把"从未存在"折进 `STATUS_DETACHED`。**DP-026 已确认空场试次存在**，必须拆出 `never_occupied`：操作上都是排除，但科学含义不同（空场是布置/录制问题，脱落是实验失败须上报），且 **G10 的两类正样本数已确认不同**（never_occupied 1 个 / 脱落 0 个，见 DP-034）。不拆则两类混为一个门，用 1 个空场正样本冒充"脱落检测已验证"——那是虚假验收 |
| DP-032 | **open（要 Mac 侧跑一次）** | 道俊 / 实现 agent | — | **`STATUS.md` 的"v4/v7 隔间4"记法有歧义，可能藏着一个 G10 假阳性。** 该行记 8-24 跨视频泛化检查时有隔间-批被判"只有 1–5 px 尾链、无身体 ⇒ 真实脱落/未悬挂"。两种读法：**①「7 个视频里的第 4 个」= 1 个隔间**，则与道俊 9-03 的确认完全相容；**②「第 4 个和第 7 个视频」= 2 个隔间**，则其中一个是**假阳性**——道俊已确认全批只有 `20mg_3周-ch4` 是空的、其余视频末帧都有老鼠。**我用同段落的"有动物的 42 个隔间-批"反推做不出唯一解**（7×4=28 个槽位，42 无法整除到任一批次数），仓库里**没有 v1..v7 → 文件名映射表**，沙箱不能解码视频，**因此本条在沙箱内无法判定，必须 Mac 侧跑**。要做的事：跑一次标定探针，**打印 v1..v7 与文件名的对应表**，并对全部 7 个视频的隔间 4 重跑标定，报出每个隔间的判定与像素证据。若为读法②，须按 G10 记一个假阳性并修——**我自己写的 spec 要求 G10 假阳性为 0** |
| DP-033 | **done（道俊 2026-09-03 拍板：选 (a)）** | 道俊 | — | **拍板结果：照常出货，报告明确写"零正样本、未验证、仅按定义实现"，功能标 `experimental`；对外不得把"尾巴攀爬检测"列为已验证的差异化点。** 落地要求：验收报告 G10c 保持挂起、κ 显示"未定义（出现率 0）"而非 1.0、SOP 里这一项照旧要勾。以下为原始分析 —— **`tail_grasp`（前爪抓尾）零正样本，尾巴攀爬检测在 v1 无法验证。** 道俊 2026-09-03 目检确认：**27 个试次里没有一只够到自己的尾巴**。后果：① 该原语出现率 = 0，κ 无法计算（**已修**：`agreement_report` 加 `undefined_no_positives`，κ 置 NaN，CLI 打印"κ=未定义"。此前 `Cohen_kappa` 在零出现率下返回 **1.000**，会被读成"抓尾判断完全一致"——那是假的）；② **"尾巴攀爬检测"此前被我列为对 CSI 的三个差异化点之一，现在必须降权**——不能声称已验证。三个选项：**(a)** 照常出货但报告明确写"零正样本、未验证、仅按定义实现"（**推荐**，诚实且不拖进度）；**(b)** 专门录一批易攀爬品系/月龄的视频拿正样本（有成本，但这是唯一能真验证的路）；**(c)** 只做合成正样本验证（能测代码路径，**不能**测判据是否符合真实攀爬形态，不可单独作为验收依据）。**注意 SOP 里这一项照旧要勾**——正是因为零出现，才要持续记录 |
| DP-034 | **open** | Capy | — | **G10 必须拆成两个子门，现在的写法用 1 个空场正样本冒充了脱落检测的验证。** 实测正负样本分布（道俊 9-03 确认后已确定）：**G10a 从未有动物**——正样本 1（`20mg_3周-ch4`）、真负样本 27，召回与假阳性**都可测**；**G10b 中途脱落**——正样本 **0**（所有其他视频末帧都有老鼠），**召回不可测，只能报假阳性 = 0**。⇒ 报告里必须分开写，**不得**用 G10a 的通过声称"脱落检测已验证"。DP-005（缺失的 `30mg_2周-ch4`）是 G10b 唯一可能的正样本来源。实现前置：DP-031 拆状态码 |
| DP-035 | **open（新增验收门 G11）** | Capy → 实现 agent | — | **总时长一致 ≠ 判断一致，G7/G8 有被"时间上错、总量对"的软件蒙过去的漏洞。** 实测：`10mg_2周-ch2` 两位评分员总时长只差 **0.9 s**（任何总量指标都判完美），但逐秒 **Jaccard 仅 0.51**；13 个试次平均 Jaccard **0.738**，远差于 r=0.917 / ICC=0.818 给人的印象——r 与 ICC 只看两个标量的协变，**对"哪些秒在动"完全不敏感**。⇒ 补 **G11：软件-人工逐秒重叠 Jaccard ≥ 人工-人工 Jaccard（当前具体门槛 0.738）**，与 G7/G8 同时报告，任一不过即不过。工具已就绪：`depressionplex/cli/scorer_disagreement.py`。证据全文见 `docs/分析_评分员分歧定位_2026-09-03.md` |
| DP-036 | **done（SOP v1.2 已出）** | Capy | `chore/scorer-disagreement` | **28.6 s 评分员间偏差已定位：全在"松手"一侧，且 SOP v1.1 的按键规则不可执行。** 起因是同事反馈"刚抬起来发现安静时间很短，不抬起来发现安静的时间很长，**没法预判**"——判断停顿长短只能在停顿结束后做出，而按键决定必须在停顿开始时做出，v1.1 的"约半秒以内不松手"**要求预测未来**。实测四条：① 起始边界两人中位差 **+0.06 s**（56% 为正 = 抛硬币），结束边界中位 **+0.47 s**（**73% 为正**）⇒ 按下是反应、松手是判断；② 统一合并间隙 g=0.5 s 只吃掉偏差的 **6.6%**，逐试次反解 g 的**变异系数 74%** ⇒ **不是 bout 颗粒度伪影**；③ 王娟独有 516.8 s 分布在 **688 条碎片**（中位 0.37 s，>5 s 仅 12 段）⇒ 可排除"把整体刚体摆动当成在动"这类定义分歧；④ 分歧呈**倒 U 形**，按活跃度排序后大分歧试次恰占连续第 4–11 名（p≈0.005）⇒ 与"需要预测的次数"最多的地方吻合。产出：`SOP_悬尾实验人工评分_v1.2.md`（按键规则整条推翻：一停就松、一动就按、不要预判；合并改为**事后声明式参数**，对人工与软件施加同一个值）+ `分析_评分员分歧定位_2026-09-03.md` + `cli/scorer_disagreement.py`。**v1/v1.1 不追溯、不删改** |
| DP-037 | **open（零成本，给实现 agent；`53f9817` 上确认尚未实现——`lovo_cv.py` 里 `onset`/"起始" 出现 0 次，属立项晚于派工单，不是漏做）** | 实现 agent | `feat/human-agreement` | **DP-013 的 LOVO-CV 目标函数加一个变体，检验 θ_mob 是不是在追人的松手习惯。** 依据 DP-036：人对"运动**起始**时刻"高度一致（中位差 0.06 s），对"**结束**时刻"不一致（0.47 s，73% 单向）。⇒ 拿"immobility 总量差"当目标函数，等于把人的**松手预测方差**写进阈值。做法：现有目标（最小化 immobility 总量差）之外，再跑一次**最小化运动起始时刻匹配误差**，比较两次拟合出的 θ_mob。两者接近 ⇒ θ_mob 稳；差很多 ⇒ 现在的拟合目标主要在追松手习惯，须改。**零额外人力成本，数据已在库**。同时提醒：`bouts.py` 的**进入判据可用人工数据校验，退出判据不可以** |
| DP-038 | **open（硬件/SOP 侧，一句话观察）** | 道俊 | — | **28 个隔间槽位里 2 个异常**（`20mg_3周-ch4` 全程无鼠、`30mg_2周-ch4` 记录缺失），**两个都在隔间 4**。可能是巧合（n=2），也可能是布置流程的系统性问题（边位、放置顺序末位易漏、该位光照或胶带条件差）。**未确认**，但成本为零：下批录制前确认一下隔间 4 的布置与检查流程即可。若属系统性，属于采集端问题，比软件侧任何优化都便宜 |

