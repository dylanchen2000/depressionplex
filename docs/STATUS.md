# DepressionPlex STATUS

> **快照，不是日志。** 永远只反映当前状态。过程记录在 `docs/ISSUES.md` 台账、
> `docs/派工单/` 的复核意见、以及 git 历史里（本文件 2026-09-03 那一版是旧时代的日志，
> 内容没丢，`git log docs/STATUS.md` 查得到）。
> 最后更新：**2026-09-16**（Account B —— DP-082 入库归并落地；人工重测一致性第一次有了实测数）。

## 一句话现状

**桌面端产品线四个 PR 全部合入 main，M2 第一次真的装得出来**（安装包 97.8 MB，
里面带 ffmpeg 和中文字体，GUI 冻结后自检 rc=0）。**科学侧仍未过验收门** ——
G7 实测 r=0.7641（门 0.818）、G8 只有 5/26 个试次达标（门 17.7 s）。
**能装 ≠ 能出数**，这两件事在报告里是分开写的。

## 主线目标

做 FST（强迫游泳）+ TST（悬尾）的商业行为分析软件，参数口径对标 CSI DepressionScan，
并在三处超越（见 `README`）。**起点范式：TST**（无水面反光/折射、背光板的理想场景、
最强差异化点正在 TST）。

**我们不是在做实验，我们是在开发产品。准是第一位的。**

---

## 一、产品线（桌面端）：四个 PR 全部合入

| PR | 内容 | 合入 | 复核意见 |
|---|---|---|---|
| #98 | DP-111 模式徽章：资质声明进状态栏，七页可见 | `a67df08` | `B8_复核意见1_DP-111.md`（从零补的，它原来一次都没被复核过） |
| #97 | DP-109 采集自检页 + `acq-check` CLI | `1d4d03e` | 复核 1/2 + **复核 3 是重做版**（A 账号写的那一份随沙箱隔离丢了，不可恢复） |
| #95 | DP-108 Windows 打包 + ffmpeg 随包 | `df7a8d5` | `B10_复核意见4_DP-108.md`（这一步整包外派，我只做复核 + 集成） |
| #96 | DP-110 导出与报告（xlsx + 审计包 + PDF） | `710203e` | 复核 1–3 + `B6_复核意见4_DP-110.md`（C0–C14 收口 + 19 处变异表） |

**2026-09-15 追加（DP-120）**：采集自检页原来点不动（`# TODO B10+` 占位），现在接上了真子进程 —— argv 在 `services/engine.py` 拼、退出码在 `models/self_test.py` 判、进度行在 `services/progress.py` 解成人话，页面只搬运。**退出码 2（测不出）当结果看，不当失败。**

合入顺序是 `#98 → #97 → #96 → #95` 定的依赖，四条都用 `git merge main` 解冲突，
**没有一次 rebase、没有一次 force-push**。

### M2 安装包状态：**可安装、可导出中文 PDF**

| 项 | 值 |
|---|---|
| 版本号 | **1.0.0**（软件版本；与「有没有计量资质」是两件事，不许混） |
| artifact | `DEPRESSION-PLEX-Setup-1.0.0`，**102,578,102 字节（97.8 MB）** —— main 上的那一次 |
| 判据 | `Build Windows Installer` **run `34941011458`（main `710203e`）success**；合入前在 `4158ccb` 上是 run `34938292858`，16 个命名步骤全绿 |
| 后端 exe smoke | `--help` rc=0 且输出含「用法」（一行同时证明 exe 能启动、`force_utf8()` 生效、中文没被 cp1252 打死）、无参数 rc=0、未知子命令 rc=2 |
| GUI exe 自检 | 冻结后七页逐页构造 OK，`SELF-TEST OK pages=7`，`ExitCode = 0`，皮肤 `app/styles/dark.qss` 已加载 |
| 随包 | ffmpeg / ffprobe（DP-108）+ **Noto Sans SC（OFL）**（DP-110）；二进制都不进 git，靠 `packaging/fetch_*.py` 下载 + sha256 校验 |
| numpy | 打包环境钉死 `1.26.4`（G7/G8 那批读数就是在它上面取的）；`pyproject` 的 `numpy>=1.24` 是「能跑起来的下限」，两个不同的事实 |

**这是三轮以来第一次「真构建成功次数 > 0」。** 前三次构建的 success 里有两次是假的：
GUI exe 是 windowed 子系统，pwsh 裸调不等它结束、`$LASTEXITCODE` 恒为 0（DP-123），
而同一份日志里印着「自检不通过：皮肤缺失」（DP-122）。门改成 `Start-Process -Wait -PassThru`
之后同一个错立刻判红。**「沙箱全绿」和「exe 能跑」是两件事。**

### 中文 PDF：两个平台都真渲染出来了

`Desktop Self-Test`：main `710203e` 上是 run `34940934935` success（两个 job）；
合入前 `4158ccb` 上 run `34938282973` 打出的字节数是 ubuntu `PDF OK: 119891 bytes`、
windows `79707 bytes`，两边 xlsx 都是 8,720 字节。
**windows-latest 上系统字体 family 枚举是 0 个**，靠随包 Noto Sans SC 出的 PDF ——
这就是「交给国内实验室的产品，能不能出 PDF 不该取决于对方装了什么字体」的落地证明。

导出侧还有三条约定已长牙：三份产物**原子性**（先渲染到临时目录、全成功才移动，
部分失败明说「一个文件都没有导出…一起撤回了」，不许出现「成功：」）；
合规声明**三处同源**（审计包 / xlsx / PDF 逐行对账）；
声明里的关键句**逐字钉住**（改「没有计量资质」一个词必红）。
计量版声明文案故意留成 `NotImplementedError` —— **这不是欠账，是闸**。

---

## 二、验收门与最近一次实测：**还没过**

门的当前值以 `docs/ISSUES.md` 台账为准（代码里 `G7_MIN_R=0.818 / G8_MAX_BIAS_S / G11_MIN_JACCARD=0.738`
写死并注明来源），**三门捆绑判定：任一不过即不过；算不出按不过处理，「不报 ≠ 过」写进报告行。**

| 读数 | 值 | 说明 |
|---|---|---|
| 本产品 试次数 / 配对数 | 27 / 26 | 27 段素材全部出数字，26 个与人工真值配对 |
| G7 软件-人工 r | **0.7641**（门 0.818） | 门就是人工一致性上限（ICC 0.818），**门不该高过人自己** |
| G8 逐试次达标 | **5 / 26**（门 17.7 s） | 总体偏差 +1.74 s、斜率 0.933、MAE 35.15 s、误差区间 −72.5 ~ +68.5 s |
| 评分员之间偏差 | 中位 19.33 s / 最大 66.79 s | 人自己也差这么多，这是门的天花板 |
| CSI 参考（27 段） | r 0.842、偏差 +0.15 s、MAE 24.63 s | 对标对象的读数 |
| CSI 空杯 | 466.88 s / **99.923 %** 判为不动 | 见下方阻塞：G10 空杯对照怎么用还没定 |

这 16 个读数**不写死在代码里**，在 `data/validation_readings.json`，每一条带
`{file, anchor, quote}` 三件套，来源文件改一个字就红。产品的合规声明直接印这些数字。

**总体偏差 +1.74 s 好看，但逐试次只有 5/26 达标** —— 总量对不等于时间轴对，
这正是 G7/G8 会被「时间上错、总量对」的软件蒙过去的那个漏洞，所以 G11（逐秒 Jaccard）不能撤。

## 三、科学侧现状

| 项 | 状态 |
|---|---|
| 胶带走廊标定 | 每帧启发式 → **一次标定**（`feat/tape-corridor` 已合）；相机固定 ⇒ 悬挂杆与胶带列位置标定一次即可 |
| P2 采集硬门 | 实测**全部通过且余量很大**：对比度约 CSI 参考的两倍；真静止动物面积抖动 0.0035，门槛 0.02（余量 6 倍）。冻结读数在 `tests/fixtures/p2_reference.json`（DP-116），门槛值在 DP-117 |
| 分割 | S1 阈值分割够用，**不需要上 S2 学习式分割**（有实测支撑） |
| θ_mob | **0.0175**，不许动。合成数据推的 0.015–0.02 与真实素材实测互相印证 |
| 人工评分 | 四人两组，A 组 ICC = 0.818（唯一预先登记的值）；SOP 已到 **v1.2**（细颗粒度按键 + 事后声明式合并）；v1/v1.1/v1.2/v1.3/v1.4 文件**不得删改** |
| 计分窗口 | DP-004 已由数据自证关闭（39 个有 holds 的试次里最大 hold 结束时间无一超过 360.00 s） |
| 尾巴攀爬 | 按「出货但不声称已验证」处理（道俊拍板，DP-033） |
| 人工真值入库 | **已按 DP-124 口径落地**（DP-082）：唯一键 `(scorer_id, trial_id)`，读数不同**报错停机**。`incoming/` 116 条读数 → **88 条真值 + 27 条已声明复评 + 1 个同源副本**；归并前 `build_table` 把 116 条当 116 场，下游 FST 数字全部虚高 |
| 组内重测（同一人重评同一场） | **第一次有实测数，而且不乐观**：27 对里 **26 对第二次读数更短**（不动时间更长），中位 **−31.74 s**，范围 −116.69 .. +123.85 s。按人：陈璇 中位 −47.42（最大差 123.85 s ≈ 窗口的三分之一）、张咸明 −22.18、徐乐彤 −14.61。**倍速在配对之间没变过，所以不是倍速的锅**。这 27 对是工具重派白送的样本，**不是设计出来的**，但它直接影响「人工能不能当金标准」以及批 B 还要不要那 8 场 |
**人工计数的目的是标定，不是当主证据。** 上面那条组内重测的数是这句话的新证据：
**同一个人隔一天重评同一场，系统性地按出更长的不动时间**，方向一致（26/27）说明这不是随机噪声。
归因还没做（DP-082 只入库、不归因），在归因之前**不许拿它去调 θ_mob、也不许拿它当「人工不可靠」的结论用**。
原语真值那批数据绝对不能放弃；
**不好让标注员返工** —— 人工标注是单次机会，工具和派工单必须一次做对。

## 四、阻塞

**2026-09-15：原来压在这里的三条口径都定了**（道俊授权我代拍其中一条），现在只剩「等素材」和三条老账。

| # | 内容 |
|---|---|
| 1 | ~~四份重复的 TST 重评算哪一份~~ **已定，且问题的前提是错的**（DP-124）：那四份**不重复**，是两批不同试次（trial_id 零重叠），`(1)` 是 DP-077 那个「导出后缀不带时刻」的伤口，浏览器加的 `(1)` 救下了第二批。**全部取并集，两人各 12 条**，唯一键 `(scorer_id, trial_id)`，`presentation_order` 不参与去重 |
| 2 | **等素材**（DP-126，清单已出）：① 一份 13 个 Motion 值**互不相同**的 `.SET`（建议 21–34 里挑 13 个）+ 用它跑出的结果；② 任意 1 段素材的 `.FSR` 逐帧文件；③ 当时的 CSI 版本号与 `.CLB`。**没有它，旋钮映射只能靠猜、逐秒对账根本做不了** |
| 3 | ~~CSI 空杯 99.923% 怎么进 G10~~ **已定**（DP-125，道俊：「空杯肯定显示空杯」）：空杯的每个空隔间必须判为**未检出动物**、**不出秒数**（不是 0 也不是 600，是不出数，并按声明占一行写明原因）；在空杯上**检出动物 ⇒ G10 直接不过**。这是对标 CSI 的一处**主动超越** —— 它没有「这里没有动物」这个状态 |
| 4 | **DP-055：一天没归因，就不许拿「重标门槛」当解法。** 这条压着 B2 / DP-014 的 G1–G11 报告 |
| 5 | **DP-057：不许由 agent 自行假定一个入水时刻**来让 FST 窗口「存在」 |
| 6 | PR **#58 / #57** 等用户复核（风险类改动我不自merge） |

## 五、下一步（按优先级）

1. **批 B 派工**：徐乐彤那 8 場自盲重评（09-15 之后、新 seed、对外说法是「抽查补评」）。
2. **标注工具 v1.7**（**DP-128，已出派工单 `docs/派工单/B13_秒表工具v1.7_DP-128.md`**）：跨会话不许重派已评场次（**28 个重复键的根因**）、删掉「复制种子」（line 114）、修 DP-086 的按键膨胀、改掉误导性的「已评进度」提示（103–104 / 112）、处理重看（41/92 条记录受影响，最差 218.15 s）。**这几条都要在批 B 派工前一次做对。**
3. ~~DP-082 ingest~~ **已做完**（`feat/dp082-ingest`，通过 595 失败 0，15 条变异全红）。它派生出三件**已写成派工单、外派执行**的活：**DP-127** 交叉核对按导出对账（48 条不一致里 43 条是核对方式造的）、**DP-128** 秒表 v1.7（重复键的根因在工具侧）、**DP-129** 抢救文件的评分员名「张」→「张咸明」。
4. G10 空杯对照的接线（按 DP-125 定的口径，落在分割层）；DP-084（#67）；DP-086（#68）/ DP-087（#69）；DP-075（`trial_body_length`）。
5. 归因 TST A/B 组约 44 s 的组间偏移。
6. CSI 11 旋钮映射（等 DP-126 的 `.SET`）；T-2 重切 `30mg_2周-ch4`；FST 代码 C1–C3；把 clip 从 `~/Downloads/tst_30clips/` 挪出来。（`_SCORER_IN_NAME_RE` 已单独出单 DP-129。）
7. 09-10 那两位评分员的 `.csv` 仍缺（JSON 是权威，**不阻塞**）。

## 六、接力必读（纪律）

- **本机判据是 3.11**：`/tmp/dpx311/bin/python run_tests.py`（venv：numpy 1.26.4 + openpyxl 3.1.5 + pyyaml），当前 `通过 595 失败 0`。Mac 自带 `python3` 是 3.9.6，`desktop/` 的 PEP 604 在它上面是运行期错误，会造出 52 条**假红**。**CI 的 3.11 才是最终判据；本地绿 CI 红时只有 CI 算。**
- 本仓**不用 pytest**，跑 `python3 run_tests.py`；新测试模块要登记进 `TEST_MODULES`。
- `main` **只接受合并**；一个分支一件事；首行 `<type>(DP-###): 中文摘要` ≤60 字；**不许 `git add -A`**；**不许 rebase + force-push**。
- **一个 checkout 一个写者**（DP-039）。`~/Work/dp-ui-wt`、`~/Work/dp071-wt`、`~/Work/dp073-wt` 以及主 clone `~/Work/depression抑郁绝望/depressionplex` 都**不是我的**，只在里面跑只读命令。
- 大文件不进 git：`~/Work/csi_in/`（1.6 GB）、CSI 的 `.BMP`、原始视频/掩膜/权重、字体二进制。
- **不许动的常量**：`θ_mob = 0.0175`、`ASSAY_WINDOWS`、`CSV_FIELDS`、G7/G8/G10/G11 门值。改任何一个是科学决定，要同步改台账并写理由。
- 每条新规矩都要有**踩上去会红**的测试，且要有变异证明。**「不为空」不是要求，「说的是真话」才是。**
- **没跑过数据的数字不许说出口**；核对过没变的结论，就说「核对过，没变」。
- 判断类的活（真值口径、验收门、算法、失败归因、契约裁决）不外包。
