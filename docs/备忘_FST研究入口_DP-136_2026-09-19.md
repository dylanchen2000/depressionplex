# 备忘：FST 独立研究入口与真实视频分割诊断（DP-136）

日期：2026-09-19　分支：`feat/dp136-fst-research-entry`（从 `47fe7a4` 起）
状态：**研究/原型**。不产正式 CSV、不进验收路径、不借 TST 发布与标定资格。

## 1. 这是什么

Spec A §4–§6 的 A 线第一版：一个**独立的** FST 研究入口
`python -m depressionplex.cli.fst_research`，做四件事：

1. 读**明确身份**的媒体（sha256 + 共用输入身份清单关联，多别名不猜、查不到照实记）；
2. 用通用几何工具对杯体/水线出**提案**（标未确认；CLB 里没讲清出处的数字绝不换算成坐标）；
3. 逐杯分割 + 质量诊断，区分「动物 / 水面反光波纹 / 短时丢失 / 申报空杯」四类状态；
4. 导出叠加短片（带来源 sha、时间、几何、失败原因）——**先证明我们看的是对的动物区域**，再谈候选特征。

## 2. 模块图（全部新增，路径隔离由测试钉住）

| 模块 | 职责 |
|---|---|
| `fst_research/timeline.py` | 三钟分离（媒体/分析/协议）；t0 未知 ⇒ `protocol_alignment=unknown`，不套 (120,360) 标准窗、不自动截最后 4 分钟；截断/缺口如实上报；台账缺项写 missing，不许填 0 |
| `fst_research/cup_geometry.py` | 杯体/水线提案：中值帧水体连通域 + 结构过滤（每条过滤写进 problems）+ 行梯度旁证（永不改主值，分歧写 note「脚本不选边」）；人工确认件走 `load_confirmed`（半确认/错范式/缺角色一律拒） |
| `fst_research/cup_perception.py` | 逐杯四态分区（observed/unclear/lost_short/declared_absent），分区必须闭合；短时丢失≠空杯；静态动物被背景吸收必须报；空杯只认人工申报且不输出 0 秒行为 |
| `fst_research/cup_features.py` | 候选特征：刚体平移/旋转与补偿后残差**两个都留**；光流/局部运动 `not_implemented`，不假装 |
| `fst_research/overlay.py` | 纯 numpy 画框/水线/掩膜轮廓/文字 + ffmpeg 短片写出；`refuse_in_repo` 强制产物落仓库外；半截 mp4 删除不留假证据 |
| `fst_research/record.py` | 诊断 JSON 组装/校验/原子落盘；分区不闭合即 raise；sha256、帧数来源、limits 全写进记录。**证据不覆盖**：`write_record`/叠加短片拒写已存在目标；每轮 run 独立目录 `<stem>_run_<时间戳>_<hex8>/`；`_完成清单.json` 只在**全部**产物成功后最后写（逐文件 sha256+bytes），无清单的 run 目录不得当交付证据；记录带 provenance（run_id/代码 SHA+dirty+branch/命令行）与 research_params（每个参数的起点值出处）。**时间加权尾区间（R3 ②）**：`analysis_end_frames` 必填，尾区间=[末抽样帧, 分析末端)取 `min(步长, 末端剩余帧)`——总帧数不整除 step 时按剩余帧计，**不得超过实际视频/分析范围末端**；截断时末端=最后消费帧+1 |
| `fst_research/isolation.py` | A1 路径隔离：静态 AST 审计 + 运行时哨兵，研究入口不得引用 TST 专属判据、`assay_core.rules`、CSI/lovo_cv；`--self-check` 可单独跑 |
| `cli/fst_research.py` | 入口。退出码：0=有杯看得见（或有申报空杯）；3=记录产出但没有一杯看得见；2=守卫拒绝；1=解码/编码失败；4=产物写出失败（失败时只清理**本次** run 目录，旧证据不动）。**空杯申报两条路（R3 ①）**：`--declared-empty`（物理杯号）与 `--declared-empty-channel`（通道号）互斥；通道申报必须配 `--channel-cup-mapping ch=cup` + `--mapping-evidence 依据串` 才应用，否则 `status=mapping_unresolved`、applied=[]、申报照实记录——**applied 只说明程序执行了申报，不表示映射已验证**。**确认件回灌对照（R3 ③）**：`--geometry` 时记录带 `water_body_fallback` 清单（确认件无派生水体区 ⇒ 对比度中值/面积占比分母/静态吸收检查区退回整 ROI），坐标不变的差异先对照该清单归因，不许全算到人工修正几何头上 |

共享模块顺带修了一个真 bug：`GeometryEnvelope.validate()` FST 分支原来用
`one(water_surface)`，多杯（>1 条水线）直接崩；改为 tank↔water_surface
同号配对校验（闭区间压边合法），单实例旧素材行为不变，回归测试钉住。

## 3. 怎么跑

```bash
python -m depressionplex.cli.fst_research 视频.mp4 \
    --out-dir ~/Work/fst_diag/某视频 --cups 4 \
    --manifest docs/共用输入身份清单_v1.csv
# t0 有依据时（依据字符串必填，缺了直接拒）：
#   --t0-source-s -118.0 --t0-evidence "……"
# 申报空杯（1 起）：--declared-empty 2
```

产物（全在 out-dir，**不许**在仓库里）：每轮一个 run 目录
`<out-dir>/<stem>_run_<YYYYmmdd-HHMMSS>_<hex8>/`，内含 `诊断_<名>.json`、
`几何提案_<名>.json`（仅在未给 `--geometry` 时）、`叠加_<名>_杯N.mp4`，
以及最后写出的 `_完成清单.json`。**重跑不覆盖**：同 out-dir 再跑只会新增
兄弟 run 目录，旧证据逐字节不动；任何产物写失败 ⇒ 退出码 4 且只删本次
run 目录。

## 4. 真实素材实测

### 4.1 首跑（正常1-4.mp4，2026-09-19 上午）

- 身份：sha256 `b27b176a04f1de14…`，464×262，25 fps，9868 帧（nb_frames），394.72 s；
  清单命中；t0 未在已核材料中找到 ⇒ `protocol_alignment=unknown`，覆盖走整条媒体轴。
- 分析抽帧 step=5：1974/9868 帧。四杯 observed 计数 1174 / 558 / 1974 / 1974，
  四态分区闭合，退出码 0。
- 首版提案把 9 个中间灰阶连通域全当杯（板缝/边条/分格线/挂绳阴影也落带内）。
  加三条可解释结构过滤后恰好 4 杯：宽 < 起点值 40px（实测真杯水体宽
  ≈92–95px、杂条 ≤17px，两簇间有大空档）、贴画面左右边界、纵贯整帧。
  **每条过滤逐条写进 problems**——误滤了人能看见、能改。
- 杯 1/2/4 水线 173/176/180 均有行梯度候选峰在 ±3px 内佐证；
  **杯 3 水线 91 无佐证峰**（挂绳阴影把箱体拉高，真实弯月面 ≈175）：
  记录里如实写「6 个候选峰没有一个落在水体外边界 ±3px 内（最近 y=206），
  脚本不选边」——留待人工在叠加图上确认，**不自动改**。
- 杯 1/2 各有 800 / 1416 帧 unclear（原因分布写在 `top_reasons`）：
  动物长时间不可见时的诚实状态，不是失败也不是空杯。

### 4.2 冻结研究配置跨视频批（全部 7 个清单内 FST 视频，2026-09-19 下午）

**配置冻结，先记失败再谈调参**：CLI 默认（step=5 @25fps≈5Hz、cups=4、
calib=40、dark=96/bright=150、min_area=40、roi_margin=30、lost_short 门=11
源帧、clip=100@0.4）+ 共用输入身份清单。**逐视频零调参**；7/7 退出码 0，
全部带完成清单。这是开发验证，不是盲测。

> **R3-115 ① 修订（抑郁8-10 申报空杯）**：下表 run_id 尾 `f7f42487` 是**旧假设
> 运行**——当时用 `--declared-empty 4`（物理杯号），等于把「评分员通道 ch4 = 从左
> 到右第 4 只物理杯」当成已确认映射直接应用。评审指出：**该映射仍是假设，找到依据
> 前不得当已确认应用**。已按通道语义补跑一次（run_id 尾 `497f39ce`，
> `--declared-empty-channel 4`、不给映射依据）⇒ `status=mapping_unresolved`、
> applied=[]、C4 **不被当空杯**（整段无候选 ⇒ unclear，见 §4.2 第 3 条）。**旧假设
> 运行原样保留**（P 组：证据不覆盖），两次并列。下表四杯计数仍引旧假设运行以便与
> 首跑口径对齐；C4 的申报语义以本段与第 3 条为准。

| 视频 | run_id 尾 | 四杯 observed（C1–C4） | 最差杯占比 | 杯 3 水线主值/最近候选峰 |
|---|---|---|---|---|
| 10mg 2周 | 6610d67f | 1165 / 456 / 1727 / 1883 | C2 23.8% | 81 / 202（不可靠） |
| 20mg 2周 | 6b50fa75 | 357 / 475 / 1168 / 1840 | C1 18.7% | 81 / 207（不可靠） |
| 20mg1周 | 43d557b7 | 391 / **50** / 1871 / 1798 | **C2 2.6%** | 81 / 204（不可靠）；C1 191/202 亦不可靠 |
| 抑郁4-7 | bf9bf7db | 1571 / 1630 / 1181 / 1811 | C3 65.2% | 85 / 202（不可靠）；C1 191/200 亦不可靠 |
| 抑郁8-10 | f7f42487 | 1183 / 660 / 1258 / 0¹ | C2 28.2%（C4 为申报空杯¹） | 89 / 212（不可靠）；C1 203/210 亦不可靠 |
| 正常5+抑郁1-3 | 82fdbfb7 | 1436 / 1467 / 1826 / 1828 | C1 78.6% | 95 / 212（不可靠） |
| 正常1-4 | a4b5d476 | 1174 / 558 / 1118 / 1974 | C2 28.3% | 91 / 206（不可靠） |

¹ 抑郁8-10 C4：2338 个抽样帧**全部** declared_absent（申报空杯绑定
status=applied），`behavior_seconds_semantics=empty_no_output`，不输出 0 秒。
**ch4↔物理杯 4 的对应是假设**（评分员清单只记通道号，从左到右映射未经证实）。

跨视频读出的事（都不是「恰好四杯」式的结论）：

1. **杯 3 型水线分歧 7/7 全复现**：主值 81–95 落在挂绳阴影上、最近梯度候选峰
   200–212 在水体侧，逐视频独立复现说明这是**系统性现象**而非单视频偶发；
   脚本不选边，全部留待人工在候选图 B 上定。部分视频杯 1 水线同样不可靠
   （191/202、191/200、203/210）。
2. **可见性差异是真信号**：C2 在 20mg1周 只有 2.6% observed（贴壁/静态被背景
   吸收，报 unclear 不报 0）；C1 在两个 2 周视频 18.7%/18.7–20% 量级。
   失败原因分布逐杯在 `top_reasons`（主导原因是「存在竞争连通域」）。
3. **lost_short 全视频为 0——这是观察，不是"门太松"的结论**（只读排查，未调门值）：
   - **状态路径**：lost_short 只在「一段无候选缺口**被前后两个 observed 抽样点夹住**、
     且源帧跨度 ≤ 门=11」时触发（`cup_perception._resolve_pending`）。贴端点或
     跨度算不出的缺口一律归 unclear（long_absence）。
   - **跨度分布（实测）**：7 视频逐杯跑只读排查（`_tools_20260919/lost_short_probe.py`，
     结果 `_logs_20260919/lost_short_probe.json`）——有动物的杯里**无候选缺口段 = 0**：
     非 observed 帧几乎全是「存在竞争连通域（候选歧义）」（7 视频合计 10103 行），
     这类帧**找到了候选**、走的不是无候选路径，根本进不了 lost_short。唯一的
     无候选段是抑郁8-10 C4（空杯），500 条**贴端点**（前后无 observed 夹住）⇒
     跨度不可算 ⇒ 归 unclear，不是 lost_short。
   - **判读**：lost_short=0 与「门=11 松不松」无关——真实素材里就没有
     「动物短暂消失又立刻回来」这种被夹住的短缺口。要看到 lost_short，得先有
     被 observed 夹住的 ≤11 源帧无候选缺口，本批一个都没有。**本轮不调门值**
     （调门不会凭空造出这种缺口，只会改「多长算短」的界线）。
   - **待确认**：人工几何确认（杯 3 型水线/ROI 修正）后竞争连通域会不会减少、
     会不会露出被夹住的短缺口，届时重跑排查再看，不在本轮预判。
4. 每视频 5–6 条结构过滤问题（宽 11–17px 窄连通域 vs 起点值 40px）。**是否误滤
   真杯未经人工确认**：观察到被滤的都是 ≤17px 窄条（真杯水体宽 ≈92–95px，两簇间
   有大空档），据此**看起来**是板缝/边条/分格线/挂绳而非真杯，但这是观察、不是
   确认；正常5+抑郁1-3 另有一条 **36px 宽连通域（x130..165 y248..265，画面底部）**
   被滤——宽度接近起点值、是否误滤尤其需人工看叠加图定，未决。**不写"无误滤真杯"
   这类确认性结论**，全部留人工在候选图 A/叠加静帧上核。
5. 7/7 t0 未取得 ⇒ `protocol_alignment=unknown`；解码全部完整、截断 0。

逐杯全量明细（ROI/水线提案、参数版本、抽样密度、observed 占比、最大连续丢失、
短片名、失败原因）与人工确认材料见
`~/Work/fst_diag/人工确认材料_20260919/`：`各杯诊断摘要_20260919.md`、
每视频 `几何候选A_整杯ROI_*.png` / `几何候选B_水线_*.png`（候选峰全画、脚本不选边）、
每杯 `叠加静帧_*_杯N.png`、`_index.json`（run_id/代码 SHA/路径索引）。

## 5. 仍未验证 / 边界

1. **时间未确认**：7/7 视频 t0（入水时刻）在已核材料中均未找到 ⇒
   `protocol_alignment=unknown`，一切协议时间语义未启用。**原始实验记录里
   t0 与录像起点的关系：未取得**（已核清单 t0_status 全部
   not_found_in_checked_materials，本轮没有新的记录来源）。
2. **几何未确认**：全部提案 `confirmed=False`；人工确认件（`load_confirmed`
   形状）还没有人做过。候选图 A/B 已交付（§4.2 路径），杯 3 型水线分歧
   （7/7 复现）与正常5+抑郁1-3 那条 36px 过滤是否误滤等人工定。
3. 清单内 7 个 FST 视频已全跑（冻结配置、零调参）；**换配置或换采集条件
   必须重看叠加图并重记**，起点值不是普适常数。
4. lost_short 门=11 源帧在真实素材上零触发——**这是观察，不是"门太松"**：只读排查
   显示有动物的杯里根本没有「被 observed 夹住的无候选短缺口」（非 observed 帧几乎
   全是竞争连通域/候选歧义，走的是别的路径），唯一的无候选段（抑郁8-10 C4 空杯）
   贴端点、跨度不可算 ⇒ 归 unclear。**本轮不调门值**（详见 §4.2 第 3 条 +
   `_logs_20260919/lost_short_probe.json`）。人工几何确认后是否露出短缺口，届时重看。
5. observed 帧内的活动/不动分类**未做**（新科学口径，未经批准）；
   光流/局部运动 `not_implemented`。
6. 合成 mp4 端到端测试覆盖工程管线，**不构成科学验证**；observed 占比是
   可见性诊断，不是行为准确率。
7. **③ 确认件回灌对照（R3-115）已在合成素材上钉住**：坐标完全不变、仅从提案态切到
   确认件态（`water_body` 派生量 → None），三处计算会退回整 ROI——对比度背景中值、
   候选面积占比分母、静态吸收陷阱检查区；三条单元测试各构造一个「同一坐标/同一动物、
   仅因回退而判定翻转」的对照（`tests/test_fst_perception.py` 的
   `test_water_body_none_fallback_*`），CLI 端到端对照
   `test_cli_confirmed_same_coords_control_water_body_fallback` 证明干净合成场景计数
   一致、且记录带 `water_body_fallback` 清单。**真实素材上尚未做过确认件回灌**（还没有
   第一份人工确认几何件），故"人工修正几何带来的改变"与"water_body=None 回退带来的
   改变"在真实素材上还没分离——分离方法就是本节这套同坐标对照，等 §6 的确认件到位再跑。

## 6. 需要人工/用户的事（不在本单内自行决定）

- 在 `~/Work/fst_diag/人工确认材料_20260919/` 上确认：四杯杯号与 ROI（候选 A）、
  各杯水线（候选 B，含杯 3 型分歧——评审不能只凭报告签 y≈175，必须在图上定）、
  正常5+抑郁1-3 的 36px 过滤是否误滤真杯；确认后产出第一份人工确认几何件，
  用 `--geometry` 回灌重跑验证可 reload，并按 §5 第 7 条的同坐标对照把"人工修正"
  与"water_body=None 回退"两种改变分离；
- **抑郁8-10 空杯申报的通道↔物理杯映射（ch4→?）**：现按 R3-115 ① 保持
  `mapping_unresolved`（未应用，C4 不当空杯）。要落地这条申报，需人工给出**映射依据**
  （哪只物理杯对应评分员记的 ch4），用 `--declared-empty-channel 4
  --channel-cup-mapping 4=<物理杯> --mapping-evidence "……"` 重跑；没有依据就保持未决，
  不得把 ch4 默认成从左到右第 4 杯。旧假设运行（`--declared-empty 4`）原样保留对照；
- 提供 t0 依据（或确认「无依据」为最终状态）；
- FST 正式口径（活动/不动分类、发布资格）仍是集中决策请求事项。

## 7. 下一项最小动作

**先跑通一份真实几何确认闭环，再扩到其余视频**（R3-115 评审指定顺序）：
先交正常1-4、20mg1周、抑郁8-10 三个视频的人工材料（原画面先于候选图，见 §6 与
`_tools_20260919/make_confirmation_materials.py`）⇒ 人工在图上确认一只杯的 ROI/水线
⇒ 由脚本（不是同事手改 JSON）产出第一份确认几何件 ⇒ `--geometry` 回灌重跑 ⇒
人工复查重跑结果。这一条闭环走完、并按 §5 第 7 条的同坐标对照把"人工修正"与
"water_body=None 回退"分离后，再扩到其余杯/视频。在此之前**不改任何参数**、
**不调 lost_short 门值**、**不把通道申报的映射当已确认**。
SKIP 统计（#54）与 csi_fst_compare 行为测试（#55）独立推进，不 gate 本线。
