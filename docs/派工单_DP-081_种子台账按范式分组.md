# 派工单 DP-081：种子台账按（评分员, 范式）分组

> **给执行 agent 的**（建议 sonnet5 级别即可）。这是一个边界清楚、有测试兜底的
> 机械改动，不涉及任何真值口径判断。**凡是让你觉得"要不要顺手改一下算法"的地方，
> 一律不改，写进 PR 描述交回来。**

---

## 一、这个 bug 是什么

`depressionplex/human_agreement.py` 里 `TableResult.seeds` 按**评分员名字**做 key：

```python
seeds: dict[str, int | None] = field(default_factory=dict)
...
if header["seed"] is not None:
    result.seeds[header["scorer_id"]] = int(header["seed"])
```

一个评分员现在会交**多份**导出（悬尾一份、FST 一份、同一范式还分批次多份）。
后读到的会**静默覆盖**前一份的 seed，于是：

- 同一个人的 TST 种子和 FST 种子只留下一个；
- `seed_groups` 因此漏报「两个人用了同一个种子」。

**这个漏报已经真实发生了三次**：张咸明和徐乐彤在 09-07、09-08 三批导出里
用了同一个 seed（其中 `204129075` 出现两次），而入库校验一次都没报出来。
共享种子会让这两人的一致性偏高约 0.05 ICC，是必须留痕的测量瑕疵。

**依赖**：本任务用到 `TrialRow.assay`，它由 DP-080 提供，已在 main
（合并提交 `51a856b`）。**从 main 起分支即可，不要 rebase 别的分支。**

---

## 二、要做成什么样（这是契约，不要自己改设计）

### 2.1 `TableResult` 字段

```python
    # DP-081：按（评分员, 范式）记种子。一个人的悬尾和 FST 是两把独立的随机顺序，
    # 用同一个 key 会静默互相覆盖，导致共享种子漏报（已真实发生 3 次）。
    seeds: dict[tuple[str, str], int | None] = field(default_factory=dict)
    seed_groups: dict[int, list[str]] = field(default_factory=dict)
    # DP-081：同一（评分员, 范式）在不同批次导出里出现了不同 seed。
    # 这是**正常的**（重评批次本来就该换种子），只记账不报错。
    seed_rebatches: list[str] = field(default_factory=list)
```

- `seeds` 的 key 从 `scorer_id` 改成 `(scorer_id, assay)`。
- `seed_groups` 的**类型不变**（`dict[int, list[str]]`），但元素字符串
  从 `"王娟"` 变成 `"王娟/TST"`，即 `f"{scorer_id}/{assay}"`。
  这样 `format_report` 的输出行不用改结构。
- `seed_rebatches` 元素格式：`f"{scorer_id}/{assay}: {旧seed} → {新seed} ({文件名})"`。

### 2.2 每份导出的范式怎么取

`load_audit_json` 返回的 `header` 里**没有** assay，不要去改 header 的形状。
用它同时返回的 `rows` 推：

```python
    for p in audit_jsons:
        header, rows = load_audit_json(p, on_reject="mark")
        result.rows.extend(rows)
        assays = {r.assay for r in rows if r.assay}
        if len(assays) > 1:
            # 一份导出只可能是一个范式（工具按范式分别导出）。混了就是数据问题，
            # 报出来但不猜——不许挑一个当代表。
            result.seed_rebatches.append(f"{header['scorer_id']}: 同一份导出混了范式 {sorted(assays)} ({p.name})")
        if header["seed"] is not None and len(assays) == 1:
            _record_seed(result, header["scorer_id"], assays.pop(), int(header["seed"]), p.name)
```

抢救 CSV 那一支（`load_salvaged_csv` 的循环）同样改：按 `r.assay` 逐行记，
key 用 `(r.scorer_id, r.assay)`。

### 2.3 新增一个小助手

```python
def _record_seed(result: "TableResult", scorer: str, assay: str,
                 seed: int, fname: str) -> None:
    """把一份导出的 seed 记进台账。同 key 换了 seed ⇒ 记 rebatch，**保留最新的**。"""
    key = (scorer, assay)
    old = result.seeds.get(key)
    if old is not None and old != seed:
        result.seed_rebatches.append(f"{scorer}/{assay}: {old} → {seed} ({fname})")
    result.seeds[key] = seed
```

### 2.4 `_invert` 与 `seed_groups`

```python
    for seed, keys in _invert(result.seeds).items():
        result.seed_groups[seed] = sorted(f"{s}/{a}" for s, a in keys)
```

`_invert` 的类型注解跟着放宽成 `dict[Any, int]` → `dict[int, list[Any]]`，
**函数体不要动**。

### 2.5 `format_report`

在 `seed 分组` 那一行之后加一行：

```python
        f"seed 换批次记账: {res.seed_rebatches or '无'}",
```

**只加这一行，别改上面任何一行的措辞**——报告文本被别的文档引用。

---

## 三、测试（必须写，写在 `tests/test_human_agreement.py`）

现有测试第 295 行左右这一条会失败，**这是预期的，按新格式改期望值**：

```python
    assert res.seed_groups == {973678866: ["王娟", "陈璇"], ...}
```
改成
```python
    assert res.seed_groups == {973678866: ["王娟/TST", "陈璇/TST"], ...}
```

`data/human_scores/raw/` 现在全是悬尾，所以只是给每个名字加 `/TST` 后缀。
**先跑一次拿到实际值再填，不要凭猜**：

```bash
python3 -c "
from depressionplex.human_agreement import build_table
r = build_table('data/human_scores/raw')
print(r.seed_groups); print(r.seeds); print(r.seed_rebatches)"
```

### 必须新增的三条测试

| 测试名 | 断言什么 |
|---|---|
| `test_same_scorer_two_assays_do_not_overwrite_each_other` | 造两份导出：同一 `scorer_id`，一份 `assay="TST"`、一份 `assay="FST"`，**seed 不同**。断言 `len(res.seeds) == 2`，两个 seed 都在，且 `seed_rebatches == []`。**这条就是本 bug 的回归锁。** |
| `test_shared_seed_across_scorers_is_detected_per_assay` | 造两个人、同范式、**同一个 seed**。断言 `res.seed_groups[<seed>] == ["甲/FST", "乙/FST"]`（排序后）。 |
| `test_same_scorer_same_assay_new_seed_is_logged_as_rebatch` | 同一人同范式两份导出、seed 不同。断言 `seeds` 只有 1 个 key、值是**后读到的**那个，且 `seed_rebatches` 有 1 条、里面同时含旧 seed 和新 seed 的字符串。 |

造数据用文件里现成的 `_write_doc(tmp, records, name, **extra)`，
文档级范式靠 `assay="FST"` 传（DP-080 已支持）。
文件名要能让 `build_table` 扫到：必须叫 `timer_audit_*.json`。
不同评分员要用**不同文件名**，否则会互相覆盖。

---

## 四、不许做的事

1. **不许改 `TrialRow` 的任何字段**，不许改 `CSV_COLUMNS`。本任务不碰入库表。
2. **不许改 `resolve_assay` / `window_bound_s` / 并集算法**。
3. **不许改 `data/` 下任何文件**，不许重新生成 `recomputed/` 的 CSV。
4. **不许 `git add -A`**，逐个文件 add。
5. **不许 rebase、不许 force push。** 有冲突就停下来，在 PR 里说明。
6. 不许把 `outputs/`、`tmp/` 里的东西提交进去。
7. 发现别的 bug **不要顺手修**，写在 PR 描述的「顺带发现」段落里交回来。

---

## 五、逐条命令（在 MacBook 上执行，仓库
`/Users/dylanchen2000/Work/depression抑郁绝望/depressionplex`）

```bash
cd "/Users/dylanchen2000/Work/depression抑郁绝望/depressionplex"
git fetch origin
git worktree add -b feat/dp081-seed-ledger ~/Work/dp081-wt origin/main
cd ~/Work/dp081-wt

# —— 改代码：depressionplex/human_agreement.py 和 tests/test_human_agreement.py ——
# （按第二、三节做）

# 必须全绿才能提交
python3 run_tests.py
```

`run_tests.py` 现在是 **249 通过 / 0 失败**。改完应该是 **252 通过 / 0 失败**
（新增 3 条）。**只要有一条失败就不要提交**，把失败输出交回来。

```bash
git add depressionplex/human_agreement.py tests/test_human_agreement.py
git commit -F - <<'MSG'
fix(DP-081): 种子台账按（评分员,范式）记，共享种子不再漏报

seeds 原来按 scorer_id 做 key，一个人交多份导出时后读到的会静默覆盖，
导致 seed_groups 漏报共享种子——张咸明与徐乐彤在 09-07/09-08 三批里
共用同一 seed，入库校验一次都没报出来。

key 改成 (scorer_id, assay)，范式取自该份导出的行（DP-080 落的 assay 列）；
同一 key 换 seed 记进新增的 seed_rebatches（重评批次换种子是正常的，只记账）。
seed_groups 元素改成 "评分员/范式"，类型不变。

新增 3 条回归测试：跨范式不覆盖、跨评分员共享种子可检出、同 key 换种子记账。
MSG

# worktree 建出来时 upstream 指向 origin/main，先解掉再推
git branch --unset-upstream 2>/dev/null || true
git push -u origin feat/dp081-seed-ledger

gh pr create --title "fix(DP-081): 种子台账按（评分员,范式）记，共享种子不再漏报" --body "$(cat <<'BODY'
## 改了什么
`TableResult.seeds` 的 key 从 `scorer_id` 改成 `(scorer_id, assay)`。

## 为什么
一个评分员现在会交多份导出（悬尾一份、FST 一份、同范式还分批次）。
原来后读到的 seed 会静默覆盖前一份，`seed_groups` 因此漏报共享种子。
**已真实漏报三次**：张咸明与徐乐彤在 09-07/09-08 三批导出里共用同一 seed
（`204129075` 出现两次），共享呈现顺序会让这两人的一致性偏高约 0.05 ICC，
必须留痕。

## 验证
- `python3 run_tests.py`：<填实际数字> 通过 / 0 失败
- 新增 3 条回归测试（见 diff）
- `build_table('data/human_scores/raw')` 的 `seed_groups` 除了名字带上 `/TST`
  后缀外与合并前一致，分组关系没变

## 顺带发现
<没有就写"无"；有就写在这里，不要顺手改>
BODY
)"
```

**PR 开完就停，不要自己合并。** 把 PR 号和 `run_tests.py` 的最后三行交回来。

---

## 六、收尾（合并之后才做）

```bash
cd "/Users/dylanchen2000/Work/depression抑郁绝望/depressionplex"
git worktree remove ~/Work/dp081-wt
git branch -D feat/dp081-seed-ledger
git pull --ff-only
```

**`~/Work/dp-ui-wt`、`~/Work/dp071-wt`、`~/Work/dp073-wt` 是别人的工作区，
一个字都不要动。**
